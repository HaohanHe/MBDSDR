# MBDSDR 桌面端依赖
# 安装: pip install -r requirements.txt

# GUI 框架
PySide6>=6.5.0

# 数值计算（频谱渲染 / 解调）
numpy>=1.24.0
scipy>=1.10.0          # 解调滤波/重采样，缺失会崩

# 卫星追踪 / GNSS
sgp4>=2.22.0
skyfield>=1.45.0
pymap3d>=2.9.0
pyserial>=3.5           # GNSS 串口 NMEA

# MCP 客户端（连接 ai-sdr Mini）
websocket-client>=1.6.0

# 模拟服务器（无硬件时测试）
websockets>=12.0

# SDR 硬件接收
pyrtlsdr==0.2.93
sounddevice>=0.4.6

# 可选：SoapySDR（HackRF/Pluto/Airspy/SDRplay，需系统驱动）
# SoapySDR
