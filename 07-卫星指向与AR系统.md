# MBDSDR 卫星指向与 AR 系统

> 版本：v1.0
> 日期：2026-09-13
> 底座：Look4Sat（rt-bishop/Look4Sat, GPL-3.0）调研方案 + Python 平台无关实现
> 许可：GPL-3.0-or-later

## 1. 概述

MBDSDR 卫星指向系统把"看见卫星"与"收到卫星"统一到同一套轨道与指向数据源。它包含三层平台无关核心算法：

| 模块 | 职责 | 文件 |
|---|---|---|
| `orbit` | SGP4 轨道计算、站心指向（方位角/仰角/距离）、多普勒频偏、内置卫星 TLE 数据库 | `src/desktop/pointing/orbit.py` |
| `ar_projection` | AR 投影数学：ENU↔az/el、四元数姿态、针孔投影、视野剔除、地平线裁剪 | `src/desktop/pointing/ar_projection.py` |
| `radar_view` | 极坐标雷达图渲染（matplotlib Agg，无 GUI 依赖），输出 PNG | `src/desktop/pointing/radar_view.py` |

这三层是平台无关的纯算法，可复用到桌面端（Python/Qt）、Android（Kotlin 移植）、XR（OpenXR）。对应 Look4Sat 集成方案里的 `core:spacetime` + `core:ar-projection` + `feature:radar`。

## 2. 坐标变换链

指向计算的核心是一条完整的坐标变换链：

```
TLE → SGP4 → TEME(ECI) 位置速度
                ↓ GMST 旋转
           ECEF (地固直角坐标)
                ↓ 减观测者 ECEF
           站心 ECEF 差向量
                ↓ 旋转矩阵
           ENU (站心东北天)
                ↓ atan2
           方位角 az (0~360°, 从北顺时针)
           仰角 el (-90~90°, 从地平面向上)
           距离 range (km)
```

### 2.1 关键公式

**大地坐标 → ECEF（WGS84）：**
```
N = a / sqrt(1 - e²·sin²(lat))
X = (N + alt)·cos(lat)·cos(lon)
Y = (N + alt)·cos(lat)·sin(lon)
Z = (N·(1-e²) + alt)·sin(lat)
a = 6378.137 km, e² = 0.00669437999014
```

**GMST（格林尼治平恒星时）：**
```
GMST = 280.46061837 + 360.98564736629·(JD-2451545.0) + 0.000387933·T² - T³/38710000
T = (JD-2451545.0)/36525
```

**ECI → ECEF（绕 Z 轴旋转 -GMST）：**
```
X_ecef = X_eci·cos(GMST) + Y_eci·sin(GMST)
Y_ecef = -X_eci·sin(GMST) + Y_eci·cos(GMST)
Z_ecef = Z_eci
```

**ECEF → ENU：**
```
E = -sin(lon)·dx + cos(lon)·dy
N = -sin(lat)·cos(lon)·dx - sin(lat)·sin(lon)·dy + cos(lat)·dz
U = cos(lat)·cos(lon)·dx + cos(lat)·sin(lon)·dy + sin(lat)·dz
```

**ENU → az/el/range：**
```
az = atan2(E, N) mod 360°
el = asin(U / range)
range = sqrt(E² + N² + U²)
```

### 2.2 距离变化率（多普勒）

```
range_rate = (dx·vx + dy·vy + dz·vz) / range
```
其中 (vx, vy, vz) 是卫星 ECEF 速度（经同样 GMST 旋转）。

多普勒频移：
```
f_observed = f_transmit · (1 - range_rate / c)
c = 299792.458 km/s
```
range_rate > 0（卫星远离）时频率降低，range_rate < 0（接近）时频率升高。

## 3. SGP4 轨道计算

### 3.1 依赖

- Python `sgp4` 库（`pip install sgp4`），提供 `Satrec.twoline2rv()` 和 `sat.sgp4(jd, fr)`
- 无需网络，纯本地计算

### 3.2 内置卫星数据库

内置 8 颗常见卫星的 TLE（2024 年数据，用于离线演示）：

| 卫星 | 类别 | 下行频率 | 说明 |
|---|---|---|---|
| ISS (ZARYA) | 空间站 | 145.800 MHz | 国际空间站，业余无线电 SSTV/语音 |
| NOAA 19 | 气象 | 137.100 MHz | APT 模拟气象云图 |
| NOAA 18 | 气象 | 137.9125 MHz | APT 模拟气象云图 |
| METEOR M2 | 气象 | 137.100 MHz | LRPT 数字气象云图 |
| FUNCUBE-1 (AO-73) | 业余 | 145.935 MHz | 线性转发器 |
| XW-2D | 业余 | 145.870 MHz | 中国业余卫星 |
| BDS-3 G1 (北斗) | 导航 | — | 北斗地球静止轨道 |
| QZS-1 (Michibiki) | 导航 | — | 日本准天顶卫星 |

> 实际使用应从 Celestrak/SatNOGS 定期更新 TLE。MBDSDR 已规划 TLE 自动更新（12 小时间隔）+ 龄期显示。

### 3.3 API

```python
from pointing.orbit import load_builtin_satellites, sky_view, doppler_shift
from datetime import datetime, timezone

sats = load_builtin_satellites()
when = datetime.now(timezone.utc)
visible = sky_view(sats, when, lat=39.9042, lon=116.4074, alt_m=50, min_elevation=0.0)
# 返回按仰角降序排列的可见卫星列表，每项含 name/category/azimuth_deg/elevation_deg/
# range_km/range_rate_km_s/downlink_mhz/above_horizon

# 单颗卫星位置
pos = sats[0].position_at(when, 39.9, 116.4)
# {azimuth_deg, elevation_deg, range_km, range_rate_km_s, above_horizon, ...}

# 多普勒
corrected = doppler_shift(145.8e6, pos["range_rate_km_s"])
```

## 4. AR 投影数学

### 4.1 投影流程

```
卫星 (az, el) → ENU 单位向量
                ↓ 设备姿态四元数逆旋转
           相机坐标系向量 (x=右, y=下, z=前)
                ↓ 针孔投影
           屏幕像素坐标 (sx, sy)
                ↓ 视锥剔除 + 地平线裁剪
           visible / on_screen
```

### 4.2 姿态表示

- **四元数** (w, x, y, z)，ZYX 内旋顺序
- **yaw 顺时针为正**（符合方位角/罗盘习惯：北=0，东=90，南=180，西=270）
- 数学上绕 z 轴正旋转为逆时针，故内部取 -yaw
- Android `TYPE_ROTATION_VECTOR` 直接输出相对 ENU 的姿态四元数，可直接使用

### 4.3 设备→相机坐标变换

Android 设备坐标系：x=右，y=前（看向屏幕外），z=上
相机坐标系：x=右，y=下，z=前（看向场景）
固定变换：`v_cam = (x_device, -z_device, y_device)`

### 4.4 针孔投影

```
fx = fy = 0.5·width / tan(fov/2)
cx = width/2, cy = height/2
若 z > 0（在相机前方）：
  sx = fx·x/z + cx
  sy = fy·y/z + cy
```

### 4.5 API

```python
from pointing.ar_projection import (quat_from_euler, project_satellite,
                                     CameraIntrinsics, enu_from_az_el)

cam = CameraIntrinsics(width_px=1080, height_px=1920, fov_deg=60)
device_quat = quat_from_euler(roll_deg=0, pitch_deg=0, yaw_deg=0)  # 朝向正北
result = project_satellite(az_deg=0, el_deg=45, device_quat=device_quat, cam=cam)
# {visible, x, y, az, el, in_front, above_horizon, on_screen}
```

### 4.6 自测覆盖

- ENU↔az/el 往返（5 组角度）
- 四元数 90° yaw 旋转（北→东）
- 正北 45°卫星在朝向正北设备屏幕中心上方
- 朝东设备看正北卫星→不在视野内
- 地平线以下卫星不可见

## 5. 极坐标雷达图

### 5.1 渲染规则

- **极坐标**：方位角 0°在北（上方），顺时针；半径=仰角，90°在中心，0°在外圈
- **地平线**：外圈红色虚线（仰角 0°）
- **卫星点**：颜色按类别（空间站红/气象蓝/业余绿/导航橙），大小随仰角
- **标签**：卫星名 + 下行频率（如有），白色圆角背景
- **图例**：类别颜色图例

### 5.2 后端

matplotlib Agg（无 GUI 依赖），输出 PNG bytes 或文件。

### 5.3 API

```python
from pointing.radar_view import render_radar_png, render_radar_to_file

png_bytes = render_radar_png(visible_satellites,
                              observer_label="北京 39.9°N 116.4°E",
                              title="MBDSDR 卫星指向雷达图")
render_radar_to_file(visible_satellites, "radar.png")
```

## 6. HTTP API

### 6.1 `GET /api/pointing/sky`

返回当前天空可见卫星列表。

**参数：**
| 参数 | 类型 | 默认 | 说明 |
|---|---|---|---|
| lat | float | 39.9042 | 观测者纬度 |
| lon | float | 116.4074 | 观测者经度 |
| alt_m | float | 50 | 观测者海拔（米） |
| min_elevation | float | 0 | 最低仰角（度） |

**返回：**
```json
{
  "ok": true,
  "observer": {"lat": 39.9, "lon": 116.4, "alt_m": 50},
  "time_utc": "2026-09-13T...",
  "visible_count": 1,
  "satellites": [
    {"name": "METEOR M2", "category": "weather",
     "azimuth_deg": 22.08, "elevation_deg": 34.85,
     "range_km": 1313, "range_rate_km_s": 4.24,
     "downlink_mhz": 137.1, "doppler_corrected_mhz": 137.098,
     "above_horizon": true}
  ]
}
```

### 6.2 `GET /api/pointing/radar`

返回当前天空可见卫星的极坐标雷达图 PNG。

参数同 `/api/pointing/sky`。返回 `Content-Type: image/png`。

## 7. AI 工具

### 7.1 `pointing_sky_view`

查询当前天空可见卫星列表（方位角/仰角/距离/多普勒修正频率）。

**用途：** 卫星通信选星、过境观察、AR 指向、SDR 卫星接收前确认目标卫星位置。

**参数：** lat / lon / alt_m / min_elevation（同 HTTP API）

### 7.2 `pointing_radar_screenshot`

渲染当前天空可见卫星的极坐标雷达图并存为 PNG，返回文件路径。

**用途：** 配合视觉模型看卫星在天空的分布；给多模态模型分析指向；生成过境观察截图。

**参数：** lat / lon / alt_m / min_elevation

**输出路径：** `~/.mbdsdr/screenshots/radar_<timestamp>.png`

### 7.3 与 AI 闭环的集成

- 能力地图 category=`pointing`，中文相关度排序支持"卫星/指向/过境/雷达/AR"等关键词
- 视觉工具 `vision_inspect_spectrum` 可分析雷达图截图（多模态模型看卫星分布）
- MCP stdio 服务器自动暴露这两个工具（`tools/list` 返回 17 个工具）

## 8. 与 Look4Sat 的关系

| 维度 | Look4Sat (上游) | MBDSDR 指向系统 |
|---|---|---|
| 语言 | Kotlin/Jetpack Compose | Python（平台无关核心） |
| 轨道 | SGP4/SDP4 纯 Kotlin | sgp4 库（C 扩展，等价算法） |
| 传感器 | `ISensorsRepo` 输出 (az, pitch) | 规划 `core:pose` 6DOF 位姿融合 |
| AR | 未实现 | `ar_projection` 数学内核已完成 |
| 雷达图 | `feature:radar` 极坐标 | `radar_view` matplotlib 渲染 |
| SDR | `IRadioController` 接口 | 与 MBDSDR SDR 子系统深度集成 |
| 许可 | GPL-3.0 | GPL-3.0-or-later（兼容） |

MBDSDR 不替换 Look4Sat，而是在其算法思想之上做平台无关实现 + SDR 深度集成 + AI 工具化。Android 端可直接复用 Look4Sat 的 Kotlin 代码，桌面端用 Python 实现。

## 9. 未来扩展（Roadmap）

### 9.1 6DOF 位姿融合（`core:pose`）

分级降级策略：
- **L0 基础**：Android `TYPE_ROTATION_VECTOR`（系统 9 轴融合）→ 3DOF 姿态
- **L1 增强**：互补滤波/Madgwick/EKF 融合原始 accel+gyro+mag，加气压计高度
- **L2 6DOF**：ARCore VIO（摄像头+IMU）输出 6DOF 位姿（含位移）
- **L3 多源兜底**：GNSS 丢失时 IMU 航位推算，标注精度衰减

桌面端可通过网络接收手机姿态数据（手机作为传感器源），或用桌面端传感器（如有）。

### 9.2 AR 实景指向（`feature:ar-sky`）

- 摄像头预览 + 设备姿态 + `ar_projection` 投影 = 卫星标签叠加到真实天空
- 过境轨迹弧线、选中引导箭头、仰角环、距离/多普勒信息卡
- 磁力计"8 字校准"提示，精度差时提示远离金属干扰
- 桌面端可用模拟姿态演示（键盘控制方向）

### 9.3 XR/VR 沉浸指向（`feature:xr-sky`）

- OpenXR 跨厂商标准（Meta Quest / Android XR / PICO）
- 站心天球渲染 + 头显 6DOF pose 驱动
- 透视 AR 头显叠加真实天空（`ar-sky` 的头显版）

### 9.4 SDR + 天线转子联动（`feature:sdr`）

- 转子指向：过境期间用实时 (az, el) 驱动天线转子（hamlib `rotctld`）
- 多普勒自动调谐：按 `range_rate` 实时修正上下行频率（`rigctld`）
- 过境工作流：AOS 自动开始录音/解码/转子跟踪 → LOS 自动停止
- AR 视图选中卫星 → 一键"跟踪接收"，手机变成指向监视器 + SDR 遥控器

### 9.5 TLE 自动更新

- 从 Celestrak/SatNOGS 定期更新（12 小时间隔）
- 每颗卫星标签显示 TLE 龄期（绿/黄/橙/红四色）
- 支持 SupGP 补充星历（高精度，对覆盖卫星优先采用）
- 支持用户自定义 TLE 输入

## 10. 自测与验证

### 10.1 模块自测

```bash
cd src/desktop/pointing
python3 orbit.py          # SGP4 轨道 + 坐标变换 + 多普勒
python3 ar_projection.py  # ENU往返 + 四元数 + 针孔投影 + 视野剔除
python3 radar_view.py     # 雷达图渲染
```

### 10.2 全链路自测

```bash
cd src/desktop
python3 selfcheck.py      # 8 项全链路，含 pointing_system
```

`pointing_system` 测试覆盖：坐标变换往返、四元数旋转、AR 投影、SGP4 轨道、多普勒、雷达图渲染、HTTP 端点（sky + radar）。

### 10.3 真实数据验证

当前（2026-09-13 UTC）北京上空可见 METEOR M2 气象卫星：
- 方位角 22.08°（北偏东）
- 仰角 34.85°
- 距离 1313 km
- 远离速度 +4.24 km/s
- 多普勒修正后下行频率 137.098 MHz

## 11. 许可与合规

- 本模块以 GPL-3.0-or-later 发布
- 基于 Look4Sat（GPL-3.0）的调研方案与算法思想
- sgp4 库以 MIT 许可发布（兼容 GPL-3.0）
- matplotlib 以 PSF 许可发布（兼容 GPL-3.0）
- 内置 TLE 数据来自 Celestrak（公开数据）
