# AI定义无线电（AI-Defined Radio, AIDR）：概念定义与理论框架

**版本**：v2.0
**日期**：2026-09-12
**作者**：MBDSDR 项目组（BI4MIB）
**状态**：草案，待同行评审

---

## 摘要

软件定义无线电（Software-Defined Radio, SDR）自 1990 年代提出以来，已有 IEEE 标准定义和成熟的技术体系。然而，随着大语言模型、AI 编程工具和端侧 AI 的发展，"AI 定义无线电"（AI-Defined Radio）作为一个新概念正在浮现，但目前尚无公认的完整定义。本文首次系统地提出 AI 定义无线电的正式定义、核心特征、能力演进路线图和评估指标，并明确其与软件定义无线电、认知无线电、AI 辅助 SDR 等相关概念的边界与区别。

本文提出的定义以**"人在回路、AI 辅助自主调控"**为当前核心，以**"SDR 功能工具化"**为本质差异化标志，以**"能力可演进"**为长期方向。与认知无线电只调参数、AI 辅助 SDR 只加模块不同，AI 定义无线电将传统 SDR 的全部功能抽象为可被 AI 调用的工具——既可以被 AI 助手（如大语言模型）通过 MCP 协议调用完成任务，也可以被 AI 编程 IDE（如 Cursor、GitHub Copilot）调用用于无线电相关的编码、实验和开发。AI 的自主能力从当前的"辅助调控"（频偏校正、频率搜索、参数自适应）逐步演进到未来的"自主分析"（制式自动识别、干扰源定位、协议逆向），最终走向"自进化"（AI 自编程、自优化算法）。人始终是最终决策者，AI 的角色是增强型助手而非完全替代者。

**关键词**：AI定义无线电；软件定义无线电；认知无线电；工具化；MCP；AI编程；人在回路

---

## 1. 引言

### 1.1 问题背景

1995 年，Mitola 首次提出"软件无线电"（Software Radio）概念，其核心思想是"通过软件修改显著改变无线电的物理层行为"[1]。1999 年，IEEE 给出标准定义："软件定义无线电是一种部分或全部物理层功能由软件定义的无线电"[2]。此后二十余年，SDR 从军用走向民用，从实验室走向大众，硬件成本从数万美元降至 20 美元以下，GNU Radio、SDR++、SatDump 等开源生态蓬勃发展。

1999 年，Mitola 进一步提出"认知无线电"（Cognitive Radio, CR）概念，定义为"一种能够基于与运行环境的交互改变发射机参数的无线电"[3]。认知无线电引入了"感知-学习-自适应"的智能闭环，但二十年来其智能几乎全部落在运营商网络侧的频谱管理和资源分配上，终端侧的认知无线电始终未能大规模落地。其根本局限在于：认知无线电只能调整**参数**（频率、功率、调制方式），不能修改**算法和代码**，且决策规则是程序员预定义的。

2020 年代以来，三条技术线同时成熟：
1. **大语言模型与 AI 编程工具**：LLM 具备代码生成、推理、自然语言理解能力，AI 编程 IDE（Cursor、GitHub Copilot 等）将 AI 能力深度集成到开发工作流中；
2. **MCP（Model Context Protocol）等工具调用协议**：AI 助手可以安全地调用外部工具和数据源，实现跨应用的任务编排；
3. **深度学习信号处理**：自动调制分类（AMC）、盲信号识别、端到端通信系统学习等方向取得突破。

这三条线的交汇催生了一个新概念——"AI 定义无线电"。然而，目前该概念尚无公认定义，存在以下问题：
- 与"AI 辅助 SDR"混淆：很多工作只是在 SDR 中加入一个 AI 模块（如深度学习解调器），就自称"AI 定义无线电"；
- 与"认知无线电"边界模糊：认知无线电已有 25 年历史，新概念必须有本质区别；
- 过度激进的定义：部分工作声称 AI 完全替代人成为无线电的"定义主体"，但当前 AI 能力远未达到完全自主可靠的水平，这种定义既不现实也不安全；
- 缺乏评估指标：无法判断一个系统是否真正实现了"AI 定义"。

#### 1.1.1 真实痛点：SDR 已经无处不在，但软件还停留在上一代

今天的高端频谱仪，本质上就是一台嵌入式电脑加一块被校准过的 SDR 前端；航空管制、卫星通信、广播电视、物联网、无线电监测、业余无线电，背后都有 SDR 的身影。硬件已经成熟到 20 美元可用，但 SDR 软件仍然存在大量真实痛点：

1. **专业门槛高**：GNU Radio、SDR++ 等主流软件要求用户具备信号处理、调制解调、频谱分析的专业知识，普通爱好者和行业用户无法直接使用；
2. **功能固定死**：算法和工作流是程序员预设的，用户遇到个性化需求（如特定的干扰源观察方式、自定义解调流程）只能改源码或放弃，绝大多数用户没有这个能力；
3. **信号分析依赖经验**：找信号、识别调制样式、判断干扰来源、解码未知协议，需要多年从业经验，新手面对一片频谱无从下手；
4. **工作流繁琐割裂（"狗活"太多）**：调谐→找信号→确认频点→录制→回放→分析→生成报告，每一步都需要人手动操作、手动判断、手动串联工具。这些繁琐、重复、不需要人类动脑的劳动（"狗活"）占据了用户绝大多数时间，真正需要人类决策和创造性判断的环节反而被淹没；
5. **跨平台体验不一致**：桌面端功能完整，移动端因驱动和算力限制功能严重缩水，用户在不同设备上获得的是完全不同的产品；
6. **AI 能力未被充分利用**：现有"AI+SDR"工作只是把 AI 作为一个附加模块（如深度学习解调器），没有把 AI 作为核心交互方式和自主决策主体。

AIDR 的核心价值主张是**把 SDR 操作里的"狗活"自动化**：那些繁琐、重复、不需要人类动脑的劳动——找信号、确认频点、调参数、录制、回放、基础分析——由 AI 自动完成，人只需要说"我要做什么"，然后专注于决策和创造性判断。用自然语言交互降低专业门槛，用工具化与可编程性让功能可组合可修改，用 AI 辅助自主调控减少手动操作，用 AI 自动分析替代经验依赖，用统一的全平台架构保证一致体验，用开源让高端频谱仪的能力人人可用。**好的工具是自明的——用户用了就知道它解决了什么问题，不需要额外去证明它的价值。**

### 1.2 本文贡献

本文首次系统地提出 AI 定义无线电的完整理论框架，贡献包括：
1. **正式定义**：给出 AI 定义无线电的严谨定义，明确"人在回路、AI 辅助"为当前核心，避免过度激进的"AI 完全自主"表述；
2. **五大核心特征**：提炼 AI 定义无线电区别于其他概念的五个本质特征——人在回路、AI 辅助自主调控、自然语言优先交互、SDR 功能工具化、能力可演进；
3. **能力演进路线图**：提出"辅助调控→自主分析→自进化主体"三层演进框架，明确当前能力边界和未来方向；
4. **工具化双端定位**：首次明确 AIDR 的工具化不仅面向 AI 助手（MCP 调用），还面向 AI 编程 IDE（用于 coding 和开发），AIDR 是开放的无线电开发与分析基础设施；
5. **概念边界**：通过对比表和判断法明确 AIDR 与 SDR、CR、AI 辅助 SDR、深度学习物理层的区别；
6. **评估指标**：提出三级评估指标体系，为工程实现和学术评估提供可操作的指导。

---

## 2. 相关概念回顾

### 2.1 软件定义无线电（SDR）

**经典定义**（IEEE Std 1900.1-2008）[2]：
> "A radio in which some or all of the physical layer functions are software defined."

**核心特征**：
- 硬件通用化：ADC/DAC 尽可能靠近天线，模拟电路最小化；
- 功能软件化：调制、解调、滤波、编解码等物理层功能由软件实现；
- 可重配置：通过加载不同软件实现不同通信标准；
- **定义主体**：**程序员**（编写软件的人）；
- **交互方式**：图形界面、命令行、配置文件，需要专业知识。

**局限性**：
- 软件是静态的，运行时行为由程序员预定义；
- 不具备学习和自适应能力（参数调整也是人工或预定义规则）；
- 交互门槛高，需要无线电专业知识；
- 功能封闭，难以被其他应用或 AI 调用。

### 2.2 认知无线电（CR）

**经典定义**（Mitola, 1999）[3]：
> "A cognitive radio is a radio that can change its transmitter parameters based on interaction with the environment in which it operates."

**核心特征**：
- 环境感知：频谱感知、信号检测、地理位置感知；
- 学习推理：基于历史经验优化决策；
- **参数自适应**：调整频率、功率、调制方式等**参数**；
- **定义主体**：**预定义算法**（感知-决策-执行循环由程序员设计）；
- 智能几乎全部落在网络侧（频谱管理、资源分配）。

**局限性**：
- 只能调整**参数**，不能修改**算法和代码**；
- 决策规则是预定义的，状态空间有限；
- 25 年来未能在消费级终端大规模普及；
- 不涉及自然语言交互和工具化调用。

### 2.3 AI 辅助 SDR（AI-Assisted SDR）

**定义**（本文归纳）：
> 在传统 SDR 架构中引入一个或多个 AI 模块（如深度学习解调器、AMC 分类器），AI 作为**辅助工具**提升特定环节的性能，但系统整体行为仍由程序员预定义的软件架构决定。

**核心特征**：
- AI 是**工具箱里的一个工具**，程序员决定何时用、怎么用；
- AI 模块通常是**预训练**的，运行时不学习；
- 系统架构、信号处理流程、接口定义全部由程序员决定；
- **定义主体**：**程序员**（AI 模块也是程序员选择和集成的）；
- 交互方式：仍然是传统 SDR 的图形界面/命令行。

**典型工作**：
- 基于深度学习的自动调制分类（AMC）；
- 基于神经网络的信号检测和解调；
- 基于强化学习的频谱感知；
- AI 辅助的干扰消除。

**局限性**：
- AI 不能修改系统架构和其他模块；
- 不具备自编程、自进化能力；
- 无法自动适配新硬件和新场景；
- 功能仍然封闭，不支持工具化调用；
- 交互方式没有本质改变。

### 2.4 深度学习物理层（Deep Learning Physical Layer）

**定义**（本文归纳）：
> 将通信系统的发射机和接收机（或其中部分模块）用端到端神经网络替代，通过数据驱动的方式联合优化波形设计和信号处理，而非依赖经典通信理论的模块化设计。

**核心特征**：
- 端到端可微、可学习；
- 数据驱动，不依赖显式的信道模型和信号处理先验；
- 通常针对特定场景（如特定信道、特定调制方式）优化；
- **定义主体**：**训练数据和优化目标**（神经网络架构仍由程序员设计）；
- 训练完成后模型是**静态**的。

**局限性**：
- 只优化**物理层特定模块**，不涉及协议栈、用户交互、工具化等；
- 训练完成后静态，不具备运行时自进化；
- 可解释性差，泛化能力有限；
- 通常是仿真研究，不基于真实 SDR 硬件。

---

## 3. AI 定义无线电的正式定义

### 3.1 定义

> **AI 定义无线电（AI-Defined Radio, AIDR）**是一种以**人在回路**为前提、以 **AI 辅助自主调控**为当前核心能力、以 **SDR 功能工具化**为本质差异化标志、具备**能力可演进**特性的新型无线电系统。在该系统中，人始终是最终决策者，AI 作为增强型助手在明确场景下执行自主调控（频偏校正、频率搜索、参数自适应等）；传统 SDR 的全部功能被抽象为可被 AI 调用的工具——既可以被 AI 助手通过 MCP 等协议调用完成任务，也可以被 AI 编程 IDE 调用用于无线电相关的编码、实验和开发；AI 的自主能力具备从"辅助调控"到"自主分析"再到"自进化主体"的演进空间，但每一阶段的能力边界和安全约束都有明确定义。

### 3.2 定义解读

这个定义包含六个关键要素：

| 要素 | 含义 | 必要性 |
|------|------|--------|
| **人在回路** | 人始终是最终决策者，AI 是增强型助手而非完全替代者 | 前提条件，确保安全可控 |
| **AI 辅助自主调控** | AI 在明确场景下自动执行调控任务（频偏校正、找中心频点、搜频率、调参数） | 当前核心能力，区别于传统 SDR 的纯手动 |
| **SDR 功能工具化** | 传统 SDR 全部功能抽象为可调用工具，支持 MCP 和 AI IDE 双端调用 | 本质差异化，区别于所有传统无线电 |
| **自然语言交互** | 用户通过极短自然语言控制无线电（"录一下""扫一下""听听144.8"） | 核心交互方式，降低使用门槛 |
| **能力可演进** | AI 能力从辅助调控→自主分析→自进化主体分层演进，每阶段有明确边界 | 长期方向，区别于认知无线电的静态能力 |
| **SDR 硬件基础** | 基于真实 SDR 硬件（ADC/DAC + 通用处理器），不是纯仿真 | 边界条件，区别于纯算法研究 |

### 3.3 与"软件定义"的本质区别

"软件定义"和"AI 定义"的本质区别在于**交互范式和能力边界**：

```
软件定义无线电（SDR）：
  程序员 → 编写软件 → 定义无线电行为
  用户 → 通过 GUI/CLI 操作预定义功能
  （软件是静态的，行为是预定义的，交互是手动的）

AI 定义无线电（AIDR）：
  程序员 → 编写 AI 框架和工具接口 → AI 辅助调控 + 工具化调用
  用户 → 通过自然语言表达意图 → AI 理解并调用工具完成任务
  AI 助手/AI IDE → 通过 MCP/工具接口直接调用 SDR 功能
  （AI 在明确场景下自主调控，功能是开放可调用的，交互是自然语言的）
```

关键洞察：
- SDR 的"定义"是**一次性的**——程序员写完软件，行为就固定了；
- AIDR 的"定义"是**持续的**——AI 在运行中持续辅助调控，用户通过自然语言持续定义需求，外部 AI 通过工具调用持续扩展使用场景；
- SDR 的功能是**封闭的**——只能在 SDR 应用内部使用；
- AIDR 的功能是**开放的**——可以被 AI 助手、AI IDE、其他应用调用，成为无线电开发与分析的基础设施。

### 3.4 与"AI 完全自主"的区别

本文明确反对"AI 完全替代人成为无线电定义主体"的激进定义，理由如下：

1. **当前 AI 能力限制**：大语言模型存在幻觉问题，在无线电这种实时性、安全性要求高的场景，完全自主不可靠；
2. **安全监管要求**：无线电发射涉及频谱监管和电磁安全，必须有人在回路确保合规；
3. **用户体验**：完全自主的系统会让用户失去控制感，"黑箱"式的自主行为难以被信任；
4. **渐进式演进更务实**：从 AI 辅助调控开始，逐步增加自主能力，每一步都经过验证，比一步到位的完全自主更可行。

因此，本文定义的 AIDR 中，**人始终是最终决策者**，AI 的角色是"增强型助手"。AI 的自主能力被严格限制在明确的、低风险的场景（如频偏校正、参数自适应），高风险决策（如发射、频率规划、协议修改）始终需要人确认。随着技术成熟和验证充分，自主能力的边界可以逐步扩展，但"人在回路"的前提不变。

---

## 4. 五大核心特征

### 4.1 特征一：人在回路（Human-in-the-Loop）

**定义**：人始终是无线电系统的最终决策者和责任主体，AI 作为增强型助手提供建议和执行低风险自主调控，但高风险决策必须经过人确认。

**具体表现**：
- **低风险自主**：频偏校正、增益调整、静噪阈值、AGC 模式、滤波器参数等，AI 可以自动调整；
- **高风险需确认**：发射操作、频率规划、协议修改、系统更新等高风险操作，AI 只能提供建议，必须人确认后执行；
- **可解释性**：AI 的每个自主调控决策都有明确的原因和依据，用户可以查看和质疑；
- **可 overridden**：用户随时可以手动覆盖 AI 的决策，切换到纯手动模式；
- **责任清晰**：AI 的自主调控在安全边界内，超出边界的操作由人负责。

**与认知无线电的区别**：
- 认知无线电的"自适应"是预定义规则驱动的，没有"人在回路"的概念——系统自己决定一切；
- AIDR 的"自主调控"是 AI 驱动的，但有明确的能力边界和人在回路的安全机制。

**评估指标**：
- 低风险自主操作的比例和准确率；
- 用户手动覆盖 AI 决策的频率（过高说明 AI 不可信）；
- 高风险操作的人确认率（应为 100%）；
- AI 决策的可解释性评分。

### 4.2 特征二：AI 辅助自主调控（AI-Assisted Autonomous Tuning）

**定义**：AI 在明确的、低风险的场景下自动执行调控任务，无需用户手动操作。这些任务具有明确的输入（信号特征）、输出（参数调整）和评估标准（信号质量提升），AI 可以可靠地完成。

**当前阶段的典型自主调控场景**：

| 场景 | 输入 | AI 操作 | 评估标准 |
|------|------|---------|----------|
| **频偏校正** | 频谱偏移、星座图旋转 | 自动估计频偏并校正 | 频偏 < 100Hz |
| **中心频点识别** | 宽带频谱扫描 | 自动找到信号中心频点和带宽 | 与真实中心偏差 < 1kHz |
| **频率搜索推荐** | 用户意图（"听听广播"） | 自动搜索活跃频率并推荐 | 推荐命中率 |
| **增益/AGC 自适应** | 信号强度变化 | 自动调整增益和 AGC 参数 | 输出音频电平稳定 |
| **静噪自适应** | 噪声电平变化 | 自动调整静噪阈值 | 噪声时静音、信号时打开 |
| **制式自动识别** | 信号特征 | 自动识别调制方式和信号类型 | 识别准确率 |
| **解调参数优化** | 解调质量指标 | 自动优化滤波器带宽、采样率等 | 误码率/信噪比提升 |
| **录制触发** | 信号出现/特定模式 | 自动开始/停止录制 | 不漏录关键信号 |

**关键原则**：
- **明确场景**：AI 只在有明确输入输出和评估标准的场景下自主调控，不做模糊的、高风险的决策；
- **可验证**：每个自主调控的效果都可以被量化验证，效果不好时自动回退；
- **可学习**：AI 从用户的手动覆盖中学习，逐步提升自主调控的准确率；
- **可关闭**：用户可以随时关闭自主调控，切换到纯手动模式。

**与认知无线电的区别**：
- 认知无线电的"自适应"范围很窄，主要是频率和功率调整，且规则是预定义的；
- AIDR 的"自主调控"范围更广（频偏、增益、静噪、制式识别、解调优化、录制触发等），且是 AI 驱动的，可以从数据中学习和改进。

**评估指标**：
- 自主调控场景的数量和覆盖率；
- 每个场景的调控准确率和效果提升；
- 用户手动干预率（越低越好，但不能为零——为零可能说明用户不关心）；
- 调控响应延迟（从信号变化到 AI 调整的时间）。

### 4.3 特征三：自然语言优先交互（Natural-Language-First Interaction）

**定义**：用户通过自然语言（尤其是极短自然语言指令）与无线电系统交互，AI 理解用户意图并调用相应工具完成任务。自然语言是首选交互方式，图形界面作为辅助和可视化手段。

**具体表现**：
- **极短指令**："录一下"（开始录制）、"扫一下"（频率扫描）、"听听144.8"（调谐到144.8MHz）、"停"（停止）、"解码"（自动识别并解码信号）；
- **意图理解**：AI 理解模糊指令，如"附近有什么"（扫描附近活跃频率并列表）、"刚才说了啥"（回放并解码最近的语音传输）、"这是什么信号"（分析当前信号并给出识别结果）；
- **多轮对话**：用户可以追问、修正、细化，AI 保持上下文；
- **场景化指令**："看飞机"（自动调谐到1090MHz并解码ADS-B）、"听卫星"（自动跟踪NOAA卫星并解码APT图像）、"找干扰"（启动干扰源检测和测向）；
- **自然语言配置**："把带宽调到12.5k"、"用NFM解调"、"增益开大点"。

**与传统 SDR 交互的区别**：
- 传统 SDR：用户需要知道具体频率、解调方式、带宽、增益等参数，通过 GUI/CLI 手动设置；
- AIDR：用户只需要表达意图（"听广播""看飞机"），AI 自动理解并配置所有参数；
- 传统 SDR 的学习曲线陡峭，需要无线电专业知识；AIDR 的自然语言交互让零背景用户也能使用。

**评估指标**：
- 自然语言指令的理解准确率；
- 极短指令（<5个字）的支持比例；
- 任务完成率（从自然语言指令到任务完成的比例）；
- 平均交互轮数（完成一个任务需要多少轮对话）；
- 零背景用户的任务完成率。

### 4.4 特征四：SDR 功能工具化与可编程性（SDR-as-Tools & Programmability）——本质差异化

**定义**：传统 SDR 的全部功能（调谐、解调、录制、扫描、解码、信号分析等）被抽象为标准化的、可被 AI 调用的工具接口；同时，系统的信号处理算法以模块化、源代码开放的方式组织，用户和 AI 都可以修改、扩展、提交新的算法模块。这两个维度共同构成 AIDR 的**可编程性**：运行时通过工具组合实现任务编排，开发时通过源代码修改实现算法定制与自进化。

**这是 AIDR 区别于所有传统无线电的本质特征。** 传统 SDR 软件的算法和功能是固定死的——用户只能在预设的 GUI 里操作，无法根据个性化需求（如特定的干扰源观察方式、自定义解调流程）修改底层行为；AIDR 从架构上保证系统是"活的"：工具可以被 AI 自由组合，源代码可以被用户和 AI 修改、测试、提交，算法可以自进化。

#### 4.4.1 双端工具化定位

AIDR 的工具化面向两类调用者：

**（1）AI 助手端（MCP 调用）**：
- AI 助手（如大语言模型、智能体）通过 MCP（Model Context Protocol）等协议调用 AIDR 的工具；
- 典型场景：用户对 AI 助手说"帮我分析一下 144.800 MHz 上有什么信号"，AI 助手调用 AIDR 的 `tune` + `scan` + `decode` 工具完成任务；
- AI 助手可以跨应用编排工作流："检测到干扰 → 调用 AIDR 测向 → 在地图上标注位置 → 生成报告"。

**（2）AI 编程 IDE 端（用于 coding）**：
- AI 编程 IDE（如 Cursor、GitHub Copilot 等）调用 AIDR 的工具，用于无线电相关的编码、实验和开发；
- 典型场景：开发者在 AI IDE 中说"写一个 FM 解调器，用 AIDR 的工具采集信号测试"，AI IDE 调用 AIDR 的 `tune` + `record` 工具采集真实信号，然后编写并测试解调器代码；
- AIDR 成为无线电开发的"实时数据来源"和"测试平台"，开发者不需要额外的硬件驱动和信号采集代码；
- AI IDE 可以调用 AIDR 的信号生成、频谱分析、误码率测试等工具，加速无线电算法的开发和验证。

#### 4.4.2 工具接口设计原则

- **标准化**：所有工具遵循统一的接口规范（输入参数、输出格式、错误处理）；
- **可发现**：AI 可以自动发现和枚举可用工具及其功能描述；
- **可组合**：工具可以组合使用，形成复杂工作流；
- **幂等安全**：读取类工具（tune、scan、decode）可以自由调用，写入类工具（record、transmit）需要权限确认；
- **流式输出**：长时间运行的工具（scan、record）支持流式输出，AI 可以实时获取中间结果。

#### 4.4.3 典型工具清单

| 工具类别 | 工具示例 | 调用者 |
|----------|----------|--------|
| **调谐控制** | `tune(frequency, mode, bandwidth)` | AI助手 + AI IDE |
| **频谱分析** | `get_spectrum()`, `scan(range, step)` | AI助手 + AI IDE |
| **信号录制** | `record(duration, format)`, `playback(file)` | AI助手 + AI IDE |
| **解调输出** | `get_audio()`, `get_demodulated_data()` | AI助手 + AI IDE |
| **协议解码** | `decode(protocol)`, `decode_adsb()`, `decode_ais()` | AI助手 + AI IDE |
| **信号识别** | `identify_signal()`, `classify_modulation()` | AI助手 + AI IDE |
| **卫星跟踪** | `satellite_track(name)`, `get_pass_info()` | AI助手 + AI IDE |
| **干扰测向** | `rdf_start()`, `rdf_get_bearing()`, `rdf_locate()` | AI助手 + AI IDE |
| **设备管理** | `list_devices()`, `open_device(id)`, `get_device_info()` | AI IDE |
| **信号生成** | `generate_signal(type, params)`, `transmit_iq(data)` | AI IDE（需权限） |
| **性能测试** | `measure_ber()`, `measure_snr()`, `benchmark()` | AI IDE |

**与传统 SDR 的区别**：
- 传统 SDR 的功能是**封闭的**——只能在 SDR 应用内部通过 GUI/CLI 使用，无法被外部程序或 AI 调用；
- AIDR 的功能是**开放的工具**——可以被 AI 助手、AI IDE、其他应用调用，成为无线电开发与分析的基础设施；
- 传统 SDR 是"终点产品"——用户打开 SDR 软件使用；
- AIDR 是"基础设施"——AI 和其他应用在 AIDR 之上构建更丰富的功能。

#### 4.4.4 可编程性：从运行时工具化到源代码级自编程

AIDR 的可编程性包含两个层次，二者共同构成"系统是活的"这一本质特征：

**（1）运行时可编程（Runtime Programmability）**：
- 传统 SDR 的操作流程是固定的 GUI 按钮，用户只能按预设路径操作；
- AIDR 将全部功能抽象为工具，AI 和用户可以通过自然语言或工具调用自由组合工具，
  形成个性化的工作流——例如"观察特定干扰源"可以由用户定义为
  `扫频检测 → 锁定干扰频点 → 录制基带 → 分析调制样式 → 生成报告`的自定义流程；
- 工具组合不需要重新编译，运行时即可编排。

**（2）源代码级可编程（Source-Level Programmability）**：
- AIDR 的信号处理算法以**模块化**方式组织，每个算法模块（滤波器、解调器、解码器、
  检测器等）有清晰的接口和独立的实现，用户可以替换、修改、新增任意模块；
- 作为全开源软件，源代码对用户完全开放，用户可以根据个性化需求实时修改源代码，
  并通过版本控制系统（Git）提交（commit）变更；
- AI 也具备自编程能力：AI 可以分析用户需求或算法瓶颈，自动生成或修改算法模块，
  在沙箱中测试，通过验证后提交变更——这是 Level 3 自进化的技术基础；
- **自进化不是凭空产生的，而是建立在模块化架构 + 源代码开放 + 可测试可提交的
  可编程性基础之上。** 没有模块化和源代码可编程，自进化就是空谈。

**可编程性的安全约束**：
- 源代码修改必须经过自动化测试（单元测试 + 回归测试 + 性能基准）验证；
- AI 自编程产生的变更必须在沙箱中测试，不直接影响主系统；
- 涉及核心模块的修改必须经过人工审核确认后才能合入主干；
- 版本管理保证每次修改可追溯、可回滚；
- 用户提交的社区贡献遵循项目的贡献审查流程。

**评估指标**：
- 工具数量和功能覆盖率；
- MCP 协议兼容性和调用成功率；
- AI IDE 集成度（是否有 IDE 插件/API）；
- 工具调用的延迟和吞吐量；
- 基于 AIDR 工具开发的第三方应用/算法数量。

### 4.5 特征五：能力可演进（Evolvable Capability）

**定义**：AIDR 的 AI 自主能力不是固定的，而是具备从"辅助调控"到"自主分析"再到"自进化主体"的分层演进空间。每一阶段有明确的能力边界、安全约束和验证标准，能力扩展是渐进的、可验证的、可回退的。

**这是 AIDR 的长期方向，不是当前阶段的必要条件。**

#### 4.5.1 三层演进框架

```
┌─────────────────────────────────────────────────────────┐
│  Level 3: 自进化主体（Self-Evolving Agent）              │
│  ─────────────────────────────────────────────           │
│  AI 自编程、自优化算法、自动逆向协议                       │
│  能力：自动写代码、自动测试、自动部署、自动回滚            │
│  安全：严格的版本管理、一键恢复、沙箱隔离、人在回路审核    │
│  时间：未来（Roadmap）                                    │
├─────────────────────────────────────────────────────────┤
│  Level 2: 自主分析（Autonomous Analysis）                 │
│  ─────────────────────────────────────────────           │
│  AI 自主完成复杂信号分析任务                               │
│  能力：制式自动识别切换、干扰源定位、跳频识别、协议逆向、  │
│        自动SSTV解码、自动联网检索解调方式、自动天线指向    │
│  安全：任务级人确认、结果可验证、操作可回滚                │
│  时间：中期（1-3年）                                      │
├─────────────────────────────────────────────────────────┤
│  Level 1: 辅助调控（AI-Assisted Tuning）                 │
│  ─────────────────────────────────────────────           │
│  AI 在明确场景下自动执行低风险调控                         │
│  能力：频偏校正、中心频点识别、频率搜索、增益/AGC自适应、  │
│        静噪自适应、制式识别、解调参数优化、录制触发        │
│  安全：参数级自动调整、效果可验证、可手动覆盖              │
│  时间：当前（Now）                                        │
└─────────────────────────────────────────────────────────┘
```

#### 4.5.2 各阶段详细说明

**Level 1：辅助调控（当前阶段）**
- AI 在明确的、低风险的场景下自动调整参数；
- 输入输出明确，效果可量化验证；
- 用户随时可以手动覆盖；
- 这是 AIDR 的当前核心能力，也是定义中的必要条件。

**Level 2：自主分析（中期方向）**
- AI 自主完成复杂的信号分析任务，不需要用户逐步指导；
- 典型能力：
  - **制式自动识别切换**：AI 扫描频谱，自动识别每个信号的制式，自动切换解调方式，用户只需要"看结果"；
  - **干扰源自动定位**：AI 自动检测干扰 → 自动控制天线指向 → 自动交叉定位 → 自动在地图上标注；
  - **跳频识别与算法提取**：AI 自动识别跳频信号 → 自动提取跳频图案 → 自动分析跳频算法；
  - **自动 SSTV 解码**：AI 自动识别 SSTV 信号 → 自动判断模式（Martin/Robot/Scott）→ 自动解码出图；
  - **自动联网检索解调方式**：遇到未知信号时，AI 自动上网搜索该信号的相关信息和解调方法，自动适配；
  - **自动天线指向**：AI 自动控制八木天线+云台，指向卫星/飞机/干扰源；
  - **VVVF 逆向**：通过电磁拾音器采集牵引系统的 VVVF 信号，AI 自动分析并逆向控制算法；
- 安全约束：任务开始前需要人确认，任务结果需要人审核，操作可回滚。

**Level 3：自进化主体（长期方向）**
- AI 具备自编程能力，可以自己修改算法、优化代码、甚至生成新的信号处理模块；
- 典型能力：
  - **自动进化算法**：AI 分析当前算法的瓶颈，自动生成改进版本，自动测试，效果好则替换；
  - **自动逆向未知协议**：AI 自动采集未知信号 → 自动分析协议结构 → 自动生成解码器 → 自动验证；
  - **自动适配新硬件**：AI 自动识别新 SDR 硬件的特性，自动生成适配驱动和优化代码；
  - **自动优化性能**：AI 根据设备性能自动调整算法复杂度，在高端设备上用复杂算法，在嵌入式设备上用简化算法；
- 安全约束（必须严格）：
  - **版本管理**：每次修改前自动快照，保留最近 N 个稳定版本；
  - **一键恢复**：检测到异常自动回滚，用户可手动恢复到任意历史版本；
  - **沙箱隔离**：AI 生成的新代码在沙箱中测试，不直接影响主系统；
  - **可审计审查流程**：自进化产生的任何代码变更必须经过自动化测试（单元测试 + 回归测试 + 性能基准）+ 人工审核确认后才能合入主干，审查记录全程可追溯；
  - **人在回路审核**：涉及核心模块修改的，必须人审核确认后才能部署；
  - **安全核心不可修改**：验证器、回滚机制、版本管理等安全模块 AI 不能修改。

#### 4.5.3 演进原则

- **渐进式**：从 Level 1 到 Level 2 到 Level 3，每一步都经过充分验证，不跳跃；
- **可验证**：每个新能力都有明确的测试用例和评估标准，通过验证才能启用；
- **可回退**：任何阶段的能力都可以关闭或回退到上一阶段，不影响基础功能；
- **安全优先**：能力越强，安全约束越严格，人在回路的审核越重要；
- **用户可选**：用户可以选择启用哪个级别的自主能力，保守用户可以只用 Level 1，激进用户可以尝试 Level 3。

**评估指标**：
- 当前启用的能力级别；
- 每个级别的能力数量和成熟度；
- 从低级到高级的演进验证通过率；
- 高级别能力的用户启用率（反映信任度）；
- 自进化的成功率和回滚率。

---

## 5. 概念边界与对比

### 5.1 对比表

| 维度 | SDR | 认知无线电 (CR) | AI 辅助 SDR | 深度学习物理层 | **AI 定义无线电 (AIDR)** |
|------|-----|-----------------|-------------|---------------|--------------------------|
| **定义主体** | 程序员 | 预定义算法 | 程序员 | 训练数据 | **人+AI协作（人在回路）** |
| **硬件基础** | SDR 硬件 | SDR 硬件 | SDR 硬件 | 通常仿真 | **SDR 硬件（必须）** |
| **智能层级** | 无 | 感知-决策（参数级） | 特定模块 | 物理层 | **辅助调控→自主分析→自进化** |
| **能调参数** | 手动 | 自动（预定义规则） | 手动/自动 | 训练时 | **AI自动（明确场景）** |
| **能改算法** | 需程序员 | 否 | 否 | 否（训练后静态） | **Level 3可（Roadmap）** |
| **能改代码** | 需程序员 | 否 | 否 | 否 | **Level 3可（Roadmap）** |
| **运行时学习** | 否 | 有限 | 否（预训练） | 否 | **Level 2+可（渐进）** |
| **自验证/自恢复** | 否 | 否 | 否 | 否 | **Level 3必须（安全基础）** |
| **自然语言交互** | 否 | 否 | 否 | 否 | **是（核心交互方式）** |
| **MCP工具化** | 否 | 否 | 否 | 否 | **是（本质差异化）** |
| **AI IDE调用** | 否 | 否 | 否 | 否 | **是（面向coding）** |
| **人在回路** | 人操作 | 不明确 | 人操作 | 人训练 | **是（前提条件）** |
| **能力可演进** | 否 | 否 | 否 | 否 | **是（三层演进框架）** |
| **典型代表** | GNU Radio, SDR++ | 频谱感知系统 | 深度学习AMC | Autoencoder通信 | **MBDSDR（本文）** |

### 5.2 关键边界判断

**一个系统是否是 AI 定义无线电？用以下五个问题判断：**

1. **是否有自然语言交互？**（用户可以用自然语言控制，而非只能用 GUI/CLI）
   - 是 → 继续；否 → 不是 AIDR（最多是传统 SDR）

2. **是否有 AI 辅助自主调控？**（AI 在明确场景下自动调整参数，如频偏校正、增益自适应）
   - 是 → 继续；否 → 不是 AIDR（最多是 AI 辅助 SDR）

3. **SDR 功能是否工具化？**（功能可以被外部 AI 调用，包括 MCP 和 AI IDE）
   - 是 → 继续；否 → 不是 AIDR（缺少本质差异化特征）

4. **是否基于真实 SDR 硬件？**（不是纯仿真）
   - 是 → 继续；否 → 不是（缺少硬件基础）

5. **人是否在回路？**（人是最终决策者，AI 是助手而非完全替代者）
   - 是 → **是 AIDR**；否 → 不是（不安全的激进设计，不符合本文定义）

**注意**：
- 问题 1-4 是**必要条件**（缺一不可）；
- 问题 5 是**前提条件**（确保安全可控）；
- Level 2/3 的能力（自主分析、自进化）是**演进方向**，不是当前必要条件——一个只实现了 Level 1 辅助调控的系统，只要满足上述五个条件，就是 AIDR（初级阶段）。

### 5.3 容易混淆的概念辨析

**Q：AIDR 和"带语音控制的 SDR"有什么区别？**
A：带语音控制的 SDR 只是把 GUI 操作换成了语音命令，本质上还是用户手动操作，AI 不做自主调控，功能也不工具化。AIDR 的 AI 会主动分析信号并自主调控（如自动校正频偏），功能可以被外部 AI 调用。

**Q：AIDR 和"有 AMC 模块的 SDR"有什么区别？**
A：AMC（自动调制分类）只是 AI 辅助 SDR 的一个模块，AI 只做分类，不做后续的自动切换和解调，功能也不工具化。AIDR 的 AI 识别制式后会自动切换解调方式、自动优化参数，且所有功能都可以被外部 AI 调用。

**Q：AIDR 和认知无线电有什么区别？**
A：认知无线电只能调整参数（频率、功率），且规则是预定义的，没有自然语言交互，功能不工具化。AIDR 的 AI 辅助调控范围更广（频偏、增益、静噪、制式识别、解调优化等），有自然语言交互，功能工具化可被外部 AI 调用，且具备能力演进空间。

**Q：AIDR 必须有自进化能力吗？**
A：不是。自进化（Level 3）是长期 Roadmap 方向，不是当前必要条件。一个实现了 Level 1 辅助调控 + 自然语言交互 + 功能工具化的系统，就是 AIDR（初级阶段）。自进化能力是 AIDR 的"可演进"特征的体现，但不是定义的必要条件。

---

## 6. 形式化描述

### 6.1 系统状态空间

定义 AI 定义无线电的状态为：

$$
S_t = (C_t, P_t, H_t, E_t, U_t, T_t)
$$

其中：
- $C_t$：系统在时刻 $t$ 的**代码和配置**（所有模块的源代码和配置参数）；
- $P_t$：系统在时刻 $t$ 的**运行时参数**（频率、增益、采样率、解调模式等）；
- $H_t$：系统在时刻 $t$ 的**历史**（过去的运行数据、性能指标、用户交互记录）；
- $E_t$：系统在时刻 $t$ 的**环境**（频谱环境、硬件状态、信号特征、地理位置）；
- $U_t$：系统在时刻 $t$ 的**用户意图**（自然语言指令、手动操作、偏好设置）；
- $T_t$：系统在时刻 $t$ 的**工具调用状态**（哪些工具被外部 AI 调用、调用结果）。

### 6.2 状态转移

传统 SDR 的状态转移：

$$
S_{t+1} = f(S_t, U_t)
$$

其中 $U_t$ 是**用户手动操作**，$f$ 是**程序员预定义**的状态转移函数。

认知无线电的状态转移：

$$
S_{t+1} = f(S_t, E_t, U_t)
$$

其中 $f$ 仍然是**预定义**的，但增加了环境感知 $E_t$，系统可以自动调整**参数** $P_t$。

AI 定义无线电的状态转移：

$$
S_{t+1} = g(S_t, E_t, U_t, T_t)
$$

其中 $g$ 是 **AI 辅助**的状态转移函数，关键区别在于：

1. **AI 辅助调控**（Level 1）：
   $$
   P_{t+1} = \text{AI\_tune}(P_t, E_t) \quad \text{（明确场景下自动调整参数）}
   $$
   AI 只调整运行时参数 $P_t$，不修改代码 $C_t$，且调整范围受安全边界约束。

2. **用户意图驱动**：
   $$
   P_{t+1} = \text{AI\_interpret}(U_t, E_t)
   $$
   用户通过自然语言 $U_t$ 表达意图，AI 理解并转化为具体的参数调整和工具调用。

3. **工具化调用**：
   $$
   T_{t+1} = \text{AI\_call\_tools}(T_t, U_t, E_t)
   $$
   外部 AI（AI 助手/AI IDE）通过工具接口调用 SDR 功能，工具调用结果反馈到系统状态。

4. **自主分析**（Level 2，演进方向）：
   $$
   A_{t+1} = \text{AI\_analyze}(E_t, H_t)
   $$
   AI 自主完成复杂信号分析任务（制式识别、干扰定位、协议逆向等），分析结果 $A_{t+1}$ 供用户决策或自动执行（低风险任务）。

5. **自进化**（Level 3，长期方向）：
   $$
   C_{t+1} = \text{AI\_evolve}(C_t, H_t, E_t) \quad \text{（需人审核 + 自动验证）}
   $$
   AI 可以修改代码 $C_t$，但必须经过自动测试验证和人审核，且有版本管理和一键回滚保障。

### 6.3 人在回路的形式化

人在回路意味着高风险决策必须经过人确认：

$$
\text{Action}(x) =
\begin{cases}
\text{execute}(x) & \text{if } x \in \text{LowRisk} \\
\text{execute}(x) & \text{if } x \in \text{HighRisk} \land \text{HumanConfirmed}(x) \\
\text{pending}(x) & \text{if } x \in \text{HighRisk} \land \neg\text{HumanConfirmed}(x)
\end{cases}
$$

其中：
- $\text{LowRisk}$：低风险操作集合（频偏校正、增益调整、静噪阈值等参数级操作）；
- $\text{HighRisk}$：高风险操作集合（发射、频率规划、系统更新、代码修改等）；
- $\text{HumanConfirmed}(x)$：人已确认操作 $x$。

### 6.4 工具化的形式化

AIDR 的功能被抽象为工具集合：

$$
\text{Tools} = \{ \tau_1, \tau_2, \ldots, \tau_n \}
$$

每个工具 $\tau_i$ 定义为：

$$
\tau_i: \text{Input}_i \rightarrow \text{Output}_i
$$

工具可以被两类调用者调用：

$$
\text{Caller} \in \{ \text{AI\_Assistant (MCP)}, \text{AI\_IDE (coding)}, \text{User (自然语言)} \}
$$

工具调用的安全策略：

$$
\text{Permission}(\tau_i, c) =
\begin{cases}
\text{allow} & \text{if } \tau_i \in \text{ReadOnly} \\
\text{allow} & \text{if } \tau_i \in \text{Write} \land \text{Authorized}(c) \\
\text{deny} & \text{otherwise}
\end{cases}
$$

---

## 7. 设计原则

基于上述定义和特征，AI 定义无线电的设计应遵循以下原则：

### 7.1 人在回路原则（Human-in-the-Loop First）

- 低风险操作 AI 自动执行，高风险操作必须人确认；
- AI 的每个决策都有可解释的原因；
- 用户随时可以手动覆盖 AI 决策，切换到纯手动模式；
- 不追求"完全自主"，追求"可靠的辅助"。

### 7.2 工具化原则（SDR-as-Tools）

- 所有功能都抽象为标准化工具接口；
- 工具接口同时支持 MCP（AI 助手调用）和 API（AI IDE 调用）；
- 工具可发现、可组合、可测试；
- 读取类工具自由调用，写入类工具需要权限确认；
- AIDR 是"基础设施"，不是"终点产品"——鼓励第三方在 AIDR 之上构建应用。

### 7.3 自然语言优先原则（Natural-Language-First）

- 自然语言是首选交互方式，GUI 作为可视化辅助；
- 支持极短指令（<5个字），降低交互成本；
- AI 理解模糊意图，自动补全参数；
- 多轮对话保持上下文；
- 零背景用户也能完成基本任务。

### 7.4 渐进式自主原则（Progressive Autonomy）

- 从 Level 1 辅助调控开始，逐步扩展到 Level 2 自主分析、Level 3 自进化；
- 每个新能力都有明确的测试用例和评估标准，通过验证才能启用；
- 用户可以选择启用哪个级别的自主能力；
- 任何级别都可以关闭或回退，不影响基础功能；
- 能力越强，安全约束越严格。

### 7.5 安全第一原则（Safety-First）

- 版本管理：每次系统修改前自动快照；
- 一键恢复：异常时自动回滚，用户可手动恢复；
- 沙箱隔离：AI 生成的新代码在沙箱中测试；
- 安全核心不可修改：验证器、回滚机制、版本管理等模块 AI 不能碰；
- 频谱合规：硬编码的频率和功率限制，AI 无法绕过；
- 所有操作可审计、可追溯。

### 7.6 开源开放原则（Open Source）

- 全部代码开源（GPL-3.0-or-later）；
- AI 模型和训练数据开源；
- 工具接口标准化，鼓励第三方集成；
- 自进化过程可审计、可复现；
- 社区驱动，欢迎贡献。

### 7.7 大众可用原则（Accessibility）

- 零背景用户也能使用（场景化一键功能 + 自然语言交互）；
- 预置大众场景（听广播、看飞机、卫星图像、干扰检测）；
- AR 可视化，"看见无线电"；
- 低成本硬件支持（RTL-SDR 等 <$20 设备）；
- 不要求用户有无线电专业知识。

### 7.8 工程实现指引（MBDSDR 参考项目）

MBDSDR 项目采用**平台无关核心 + 即插即用抽象槽位**的工程架构：核心算法与 AI 工具链
不依赖特定平台，平台相关能力通过抽象层隔离，新增平台只需实现对应接口。功能按平台能力
渐进启用，能做到的做到，做不到的明确标注功能子集并预留槽位以便未来即插即用。

#### 7.8.1 平台支持矩阵（务实分级，非承诺清单）

| 层级 | 平台 | 状态 | 说明 |
|---|---|---|---|
| **Tier 1（当前目标）** | Linux x86_64（Ubuntu/Debian/Deepin/UOS/Arch/Kali）、Windows x86_64、macOS x86_64/arm64 | 主力开发与验证平台 | 桌面全功能，Qt6 原生 + C++/Python 双栈，驱动与依赖最成熟 |
| **Tier 2（预留槽位，工程可行）** | Linux arm64/arm32（树莓派等嵌入式）、Windows arm64（WOA）、Android arm64 | 预留 HAL 与渲染后端槽位，需逐平台适配驱动 | 功能子集：核心 DSP + AI 工具链可用，部分硬件加速/高采样率受限于驱动 |
| **Tier 3（探索方向，仅预留接口）** | Ubuntu Touch、UWP、鸿蒙（仅作适配盾牌预留，非核心目标）、其他小众 RTOS | 仅定义抽象接口，不承诺功能 | 若社区有需求且驱动可行，可通过即插即用槽位接入；否则保持接口预留 |

> **科学诚实声明**：Tier 2/3 中的部分平台（如 UWP、Ubuntu Touch、某些 arm32 设备）
> 因驱动生态、GPU 支持、USB Host 权限等现实限制，可能无法实现桌面端全部功能。
> 本框架不为这些平台承诺全功能，而是要求：**能做到的做到，做不到的明确标注功能子集，
> 预留槽位以便未来即插即用。** 这与"全平台"作为营销口号有本质区别。

#### 7.8.2 即插即用抽象槽位

平台相关能力通过以下抽象层隔离，新增平台只需实现对应接口，不改动核心算法与 AI 工具链：

1. **设备抽象层（Device HAL）**：统一 SDR 设备接口（open/close/set_freq/set_gain/read_iq），
   后端可接 SoapySDR、librtlsdr、libhackrf、网络 SDR（rtl_tcp/spyserver）、虚拟设备（录制文件回放、信号发生器）；
2. **渲染后端槽位**：频谱/瀑布图渲染不硬编码 OpenGL，优先 Qt QPainter（软件渲染，全平台可用），
   可选 OpenGL ES/Vulkan/Metal 后端，WOA 等 OpenGL 不可用平台自动降级到软件渲染；
3. **音频后端槽位**：通过抽象接口接 PulseAudio/ALSA/CoreAudio/WASAPI/OpenSL/AAudio，
   无音频设备平台（无头服务器）自动降级为文件输出；
4. **网络后端槽位**：MCP 服务器支持 stdio（本地 AI IDE）与 SSE/HTTP（远程/网页端）两种传输，
   按平台网络能力选择。

#### 7.8.3 移动端功能降级原则

移动端（Android/arm 嵌入式）因以下现实限制，功能为桌面端的子集，必须诚实标注：

- **驱动限制**：Android USB Host 权限、OTG 供电、部分 SDR 设备无 Android 驱动；
- **算力限制**：移动端 CPU/GPU 算力有限，高采样率实时 FFT、多通道 DDC、重型解调可能降采样或关闭；
- **外设限制**：无标准音频输入输出、屏幕尺寸限制交互；
- **AI 模型限制**：端侧小模型能力弱于桌面端大模型，复杂自主分析任务可委托云端或桌面端。

降级策略：**核心功能（频谱查看、信号检测、基带录制/回放、AI 辅助调控）必须可用；
高级功能（多通道实时解调、高采样率、重型 AI 自主分析）按设备能力渐进启用，
不可用时明确提示而非静默失败。**

#### 7.8.4 通用 Agent 接入引导

AIDR 的 SDR-as-Tools 设计要求任何通用 AI Agent（不限于内置智能体）都能接入并调用 SDR 能力。
接入引导包括：

1. **标准协议主通道**：MCP（Model Context Protocol）stdio 为默认接入方式，
   任何支持 MCP 的 AI IDE（Claude Desktop、Cursor、豆包等）零代码接入；
2. **接入文档**：提供 `mcp_server.example.json` 配置模板、工具清单与 inputSchema 自动发现、
   常见任务编排示例（检测→录制→分析→报告）；
3. **HTTP 回退通道**：不支持 MCP 的 Agent 可通过本地 REST API（`/api/*`）调用，
   返回统一 JSON 格式，与 MCP 工具语义一致；
4. **能力自描述**：Agent 接入后可通过 `ui_capabilities` 工具查询当前平台支持的功能子集，
   避免调用不可用功能；
5. **错误可恢复**：工具调用失败返回结构化错误（`ok:false` + 原因 + 建议），
   Agent 可据此换路或降级，而非收到无意义异常。

---

## 8. 评估指标体系

为了判断一个系统是否真正实现了"AI 定义"，本文提出三级评估指标：

### 8.1 一级指标：必要条件（Pass/Fail）

| 指标 | 评估方法 | 通过标准 |
|------|----------|----------|
| 自然语言交互 | 测试用户能否用自然语言完成基本任务 | 至少支持 10 种极短指令，理解准确率 > 80% |
| AI 辅助自主调控 | 测试 AI 是否在明确场景下自动调整参数 | 至少实现 3 种自主调控场景（如频偏校正、增益自适应、静噪自适应） |
| SDR 功能工具化 | 测试功能是否可被外部 AI 调用 | 至少有 MCP 接口，暴露 10 个以上工具，调用成功率 > 95% |
| 真实 SDR 硬件 | 测试是否支持真实 SDR 设备 | 至少支持 1 种真实 SDR 硬件（如 RTL-SDR） |
| 人在回路 | 测试高风险操作是否需要人确认 | 发射、系统更新等高风险操作 100% 需要人确认 |

### 8.2 二级指标：核心能力（量化评分）

| 指标 | 评估方法 | 评分标准 |
|------|----------|----------|
| **自然语言交互能力** | | |
| 极短指令支持数 | 统计 <5 字的指令数量 | > 20 为优秀 |
| 意图理解准确率 | 测试模糊指令的理解准确率 | > 85% 为优秀 |
| 任务完成率 | 从自然语言到任务完成的比例 | > 90% 为优秀 |
| 平均交互轮数 | 完成一个任务的对话轮数 | < 2 轮为优秀 |
| **AI 辅助调控能力** | | |
| 自主调控场景数 | 实现的自动调控场景数量 | > 8 为优秀 |
| 调控准确率 | 每个场景的调控正确比例 | > 90% 为优秀 |
| 调控响应延迟 | 从信号变化到 AI 调整的时间 | < 100ms 为优秀 |
| 用户手动干预率 | 用户手动覆盖 AI 决策的比例 | 5%-20% 为合理（过低可能说明用户不关心，过高说明 AI 不可信） |
| **工具化能力** | | |
| 工具数量 | 暴露的工具接口数量 | > 30 为优秀 |
| 工具覆盖率 | 工具覆盖的 SDR 功能比例 | > 80% 为优秀 |
| MCP 调用成功率 | AI 助手通过 MCP 调用工具的成功率 | > 95% 为优秀 |
| AI IDE 集成度 | 是否有 IDE 插件/API，调用是否顺畅 | 有官方插件为优秀 |
| 工具调用延迟 | 从调用到返回结果的时间 | < 500ms 为优秀 |
| **人在回路与安全** | | |
| 低风险自主操作比例 | AI 自动执行的低风险操作占比 | > 70% 为优秀 |
| 高风险人确认率 | 高风险操作的人确认比例 | 100% 为必须 |
| AI 决策可解释性 | AI 决策是否有明确原因说明 | 全部可解释为优秀 |
| 手动覆盖便捷度 | 用户手动覆盖 AI 决策的操作成本 | 一键覆盖为优秀 |

### 8.3 三级指标：演进能力（量化评分）

| 指标 | 评估方法 | 评分标准 |
|------|----------|----------|
| **Level 2 自主分析能力** | | |
| 自主分析任务数 | 实现的复杂信号分析任务数量 | > 5 为优秀（制式识别、干扰定位、跳频识别、SSTV解码、协议逆向等） |
| 分析任务成功率 | 自主分析任务的完成比例 | > 70% 为优秀 |
| 分析结果准确率 | 分析结果与人工分析的一致率 | > 80% 为优秀 |
| **Level 3 自进化能力** | | |
| 自进化循环完整度 | Execute-Reflect-Rewrite-Test-Commit 是否完整 | 完整实现为优秀 |
| 进化成功率 | AI 生成代码通过测试并部署的比例 | > 30% 为良好 |
| 回滚机制可靠性 | 异常时自动回滚的成功率 | 100% 为必须 |
| 版本管理完整性 | 快照数量、保留策略、恢复速度 | 保留 >10 个版本，恢复 <5s 为优秀 |
| 沙箱隔离有效性 | AI 生成代码是否在沙箱中测试，不影响主系统 | 完全隔离为优秀 |
| **用户信任度** | | |
| 高级功能启用率 | 用户启用 Level 2/3 功能的比例 | > 30% 为良好（反映用户信任） |
| 用户满意度 | 用户对 AI 辅助的满意度评分 | > 4/5 为优秀 |
| 系统可用性 | 系统正常运行时间比例 | > 99% 为优秀 |

---

## 9. 讨论

### 9.1 AI 定义无线电不是什么

为了避免概念泛化，明确 AI 定义无线电**不是**：

1. **不是"带语音控制的 SDR"**：语音控制只是把 GUI 操作换成语音，本质还是用户手动操作，AI 不做自主调控，功能也不工具化。

2. **不是"加了 AI 模块的 SDR"**：在 GNU Radio 里加一个深度学习解调器或 AMC 分类器，这叫 AI 辅助 SDR，不叫 AI 定义无线电。关键区别是 AI 是否能自主调控、功能是否工具化、是否有自然语言交互。

3. **不是"认知无线电的新名字"**：认知无线电只能调参数，且规则是预定义的，没有自然语言交互，功能不工具化。AIDR 的 AI 辅助调控范围更广，交互方式更自然，功能开放可调用。

4. **不是"纯仿真的 AI 通信系统"**：必须基于真实 SDR 硬件。在 MATLAB/Python 里仿真一个端到端学习的通信系统，这不叫 AI 定义无线电。

5. **不是"AI 完全自主的黑箱系统"**：本文明确反对"AI 完全替代人"的激进定义。人始终在回路，AI 是增强型助手，自主能力有明确边界和安全约束。

6. **不是"必须有自进化能力才算"**：自进化（Level 3）是长期 Roadmap，不是必要条件。实现了 Level 1 辅助调控 + 自然语言交互 + 功能工具化的系统，就是 AIDR（初级阶段）。

### 9.2 工具化的深层意义

AIDR 的工具化（SDR-as-Tools）不仅仅是"加个 API"，它代表了无线电软件范式的根本转变：

**从"终点产品"到"基础设施"**：
- 传统 SDR 是终点产品——用户打开 SDR 软件，在软件内部完成所有操作；
- AIDR 是基础设施——AI 助手、AI IDE、其他应用都在 AIDR 之上构建功能，AIDR 提供无线电能力的"水电煤"。

**从"人适应软件"到"软件适应人"**：
- 传统 SDR 要求人学习软件的操作方式（哪个按钮调频率、哪个菜单设解调）；
- AIDR 让人用自然语言表达意图，软件理解并自动完成，AI 还会主动辅助调控。

**从"封闭生态"到"开放生态"**：
- 传统 SDR 的功能封闭在应用内部，第三方难以扩展；
- AIDR 的功能工具化后，第三方可以基于这些工具构建新的应用、算法、工作流，形成开放生态。

**AI IDE 调用的特殊意义**：
- AIDR 被 AI 编程 IDE 调用，意味着无线电算法的开发方式发生了根本变化；
- 传统方式：开发者写信号采集代码 → 连接硬件 → 采集数据 → 写算法 → 测试 → 调试，循环往复；
- AIDR 方式：开发者对 AI IDE 说"写一个 FM 解调器，用真实信号测试"，AI IDE 调用 AIDR 的工具采集真实信号、生成测试数据、编写并验证解调器代码；
- AIDR 成为无线电开发的"实时数据来源"和"自动化测试平台"，大幅降低开发门槛。

### 9.3 与"大模型定义一切"的关系

当前业界有"大模型定义一切"（LLM-defined everything）的趋势，包括大模型定义操作系统、大模型定义数据库、大模型定义应用等。AI 定义无线电是这一趋势在无线电领域的具体体现。

但无线电领域有其特殊性：
- **实时性要求高**：信号处理通常需要毫秒级延迟，大模型推理速度可能成为瓶颈——因此 AIDR 的 AI 主要做"慢决策"（调控、识别、分析），实时信号处理仍由优化后的代码执行；
- **安全性要求高**：无线电发射涉及频谱监管和电磁安全，必须有人在回路和严格的安全边界；
- **专业性强**：无线电通信有深厚的理论基础，AI 不能完全替代领域知识，而是"AI + 领域知识"协作；
- **硬件多样性**：SDR 硬件种类繁多，AI 适配需要工具化的接口和渐进式能力。

因此，AIDR 不是"大模型直接控制无线电"，而是"大模型理解用户意图 + 调用工具化的 SDR 功能 + 在明确场景下辅助调控"。AI 工作在较慢的时间尺度（秒级的决策和交互），实时信号处理（微秒/毫秒级）仍由传统 DSP 代码执行。

### 9.4 伦理与监管考量

AI 定义无线电的工具化和自主能力带来了新的伦理和监管问题：

1. **频谱监管**：AI 辅助调控可能意外调整到违规频率。必须有硬编码的频率合规检查，AI 无法绕过；发射操作必须人确认。

2. **电磁安全**：AI 自主调控增益和功率可能意外干扰其他系统。必须有硬编码的功率限制，AI 无法超出；高功率发射必须人确认。

3. **工具化滥用**：AIDR 的工具可以被 AI 调用，可能被滥用于未授权的信号监听或干扰。必须有工具调用的权限控制和审计日志；发射类工具需要严格的授权机制。

4. **可审计性**：AI 的每个自主调控决策和工具调用都必须可追溯、可审计，便于事后分析和责任认定。

5. **开源透明**：全部代码和 AI 模型开源，接受社区审查，防止隐藏的恶意功能。

6. **用户可控**：用户始终有最终控制权，可以随时关闭 AI 自主调控、切换到手动模式、限制工具调用权限。

7. **隐私保护**：信号录制和解码可能涉及他人通信内容，必须有明确的隐私提示和合法使用指引；AIDR 只应用于合法的无线电监听（如航空、海事、业余无线电等公开频段）。

---

## 10. 结论

本文首次系统地提出了 AI 定义无线电（AI-Defined Radio, AIDR）的完整理论框架：

1. **正式定义**：以人在回路为前提、以 AI 辅助自主调控为当前核心能力、以 SDR 功能工具化为本质差异化标志、具备能力可演进特性的新型无线电系统；

2. **五大核心特征**：
   - 人在回路（Human-in-the-Loop）：人始终是最终决策者，AI 是增强型助手；
   - AI 辅助自主调控：AI 在明确场景下自动执行频偏校正、频率搜索、参数自适应等低风险调控；
   - 自然语言优先交互：用户通过极短自然语言控制无线电，降低使用门槛；
   - SDR 功能工具化（本质差异化）：全部功能抽象为可被 AI 调用的工具，支持 MCP（AI 助手）和 AI IDE（coding）双端调用，AIDR 是开放的无线电开发与分析基础设施；
   - 能力可演进：AI 能力从"辅助调控"（Level 1，当前）→"自主分析"（Level 2，中期）→"自进化主体"（Level 3，长期）分层演进，每阶段有明确边界和安全约束；

3. **能力演进路线图**：三层演进框架，明确当前能力边界和未来方向，自进化是 Roadmap 而非必要条件；

4. **工具化双端定位**：首次明确 AIDR 不仅面向 AI 助手（MCP 调用完成任务），还面向 AI 编程 IDE（调用用于 coding 和开发），这是区别于所有传统无线电的本质特征；

5. **概念边界**：通过对比表和五问判断法，明确 AIDR 与 SDR、认知无线电、AI 辅助 SDR、深度学习物理层的区别，避免概念泛化；

6. **形式化描述**：用状态空间和状态转移函数描述 AIDR 的人在回路、AI 辅助调控、工具化调用和能力演进；

7. **设计原则**：人在回路优先、工具化、自然语言优先、渐进式自主、安全第一、开源开放、大众可用；

8. **评估指标**：三级指标体系（必要条件 Pass/Fail + 核心能力量化评分 + 演进能力量化评分）。

这一框架为 MBDSDR 项目的后续开发提供了理论指导，也为学术界研究 AI 定义无线电提供了共同语言和评估标准。与过度激进的"AI 完全自主"定义不同，本文提出的定义务实、安全、可落地——当前阶段聚焦 AI 辅助调控和工具化，长期逐步演进到自主分析和自进化，人始终在回路确保安全可控。

未来工作将围绕 MBDSDR 的具体实现展开，验证本文提出的定义和框架的可行性，并通过实验数据评估各项目标。同时，将积极推动 AIDR 工具接口的标准化，促进开放生态的形成。

---

## 参考文献

[1] J. Mitola, "The software radio architecture," IEEE Communications Magazine, vol. 33, no. 5, pp. 26-38, May 1995.

[2] IEEE Std 1900.1-2008, "IEEE Standard for Definitions and Concepts for Dynamic Spectrum Access: Terminology Relating to Emerging Wireless Networks, System Functionality, and Spectrum Management," 2008.

[3] J. Mitola, "Cognitive radio: making software radios more personal," IEEE Personal Communications, vol. 6, no. 4, pp. 13-18, Aug. 1999.

[4] S. Haykin, "Cognitive radio: brain-empowered wireless communications," IEEE Journal on Selected Areas in Communications, vol. 23, no. 2, pp. 201-220, Feb. 2005.

[5] T. O'Shea and J. Hoydis, "An introduction to deep learning for the physical layer," IEEE Transactions on Cognitive Communications and Networking, vol. 3, no. 4, pp. 563-575, Dec. 2017.

[6] A. Goldsmith et al., "Breaking spectrum gridlock with cognitive radios: an agenda for the future," Proceedings of the IEEE, vol. 97, no. 5, pp. 894-914, May 2009.

[7] I. F. Akyildiz et al., "NeXt generation/dynamic spectrum access/cognitive radio wireless networks: a survey," Computer Networks, vol. 50, no. 13, pp. 2127-2159, Sep. 2006.

[8] T. J. O'Shea et al., "Over-the-air deep learning based radio signal classification," IEEE Journal of Selected Topics in Signal Processing, vol. 12, no. 1, pp. 168-179, Feb. 2018.

[9] N. West and T. O'Shea, "Deep architectures for modulation recognition," in Proc. IEEE International Symposium on Dynamic Spectrum Access Networks (DySPAN), 2017, pp. 1-6.

[10] Model Context Protocol (MCP), "MCP Specification," 2024. [Online]. Available: https://modelcontextprotocol.io/

[11] MBDSDR Project, "MBDSDR: AI-Defined Software Defined Radio," 2026. [Online]. Available: https://github.com/mbdsdr/mbdsdr

---

## 附录 A：术语表

| 术语 | 缩写 | 定义 |
|------|------|------|
| 软件定义无线电 | SDR | 部分或全部物理层功能由软件定义的无线电 |
| 认知无线电 | CR | 能基于与环境的交互改变发射机参数的无线电 |
| AI 定义无线电 | AIDR | 以人在回路为前提、AI 辅助自主调控为核心、SDR 功能工具化为标志、能力可演进的新型无线电 |
| 模型上下文协议 | MCP | 允许 AI 助手安全调用外部工具的开放协议 |
| AI 编程 IDE | — | 集成 AI 能力的编程开发环境（如 Cursor、GitHub Copilot） |
| 人在回路 | HITL | 人始终是最终决策者，AI 是增强型助手 |
| 辅助调控 | — | AI 在明确场景下自动执行低风险参数调整 |
| 自主分析 | — | AI 自主完成复杂信号分析任务（Level 2） |
| 自进化 | — | AI 自编程、自优化算法（Level 3） |
| 自动调制分类 | AMC | 自动识别信号调制方式的技术 |
| 工具化 | — | 将功能抽象为可被外部调用的标准化接口 |

---

## 附录 B：MBDSDR 对本定义的实现映射

| 定义要素 | MBDSDR 实现 | 状态 |
|----------|-------------|------|
| **人在回路** | 所有高风险操作需确认，AI 决策可解释，可手动覆盖 | 架构支持 |
| **AI 辅助自主调控** | agc（自动增益）、freq_correction（频偏校正）、scanner（频率搜索）、squelch（静噪自适应） | 已实现基础 |
| **自然语言交互** | ai_operator 模块 + MCP 工具 + 自然语言控制 | 已实现基础 |
| **SDR 功能工具化** | mcp 模块，37 个工具，支持 MCP 协议 | 已实现 |
| **AI IDE 调用** | MCP 接口兼容 AI IDE，工具可用于 coding | 架构支持 |
| **Level 1 辅助调控** | agc/freq_correction/scanner/squelch/demod 参数自适应 | 已实现 |
| **Level 2 自主分析** | rdf（干扰测向）、adsb（飞机解码）、noaa_apt（卫星图像）、rds（广播数据）、ft8/psk31/rtty/sstv（数字模式） | 部分实现 |
| **Level 3 自进化** | version_manager（版本管理+一键恢复，Phase1 安全基础设施） | Phase1 完成 |
| **真实 SDR 硬件** | device 抽象层（14 种设备类型），具体驱动开发中 | 抽象层完成 |
| **安全机制** | version_manager（快照/回滚）、工具权限控制 | 基础实现 |
| **开源开放** | GPL-3.0-or-later，全部代码开源 | 已实现 |
| **大众可用** | 场景化一键功能（听广播/看飞机/卫星图像）、AR 空间指向（core 模块） | 部分实现 |

---

## 附录 C：能力演进路线图详细对照

| 能力 | Level 1（当前） | Level 2（中期） | Level 3（长期） |
|------|----------------|----------------|----------------|
| **频率管理** | 手动/AI推荐频率 | AI自动搜索并锁定活跃频率 | AI自动规划频率使用策略 |
| **解调** | 手动选择解调方式 | AI自动识别制式并切换解调 | AI自动生成未知信号的解码器 |
| **参数调整** | AI自动调整增益/静噪/频偏 | AI自动优化整套接收参数 | AI自动优化算法本身 |
| **信号分析** | 基本频谱显示 | AI自动识别信号类型并解码 | AI自动逆向未知协议 |
| **干扰处理** | 手动检测干扰 | AI自动检测并定位干扰源 | AI自动规避/消除干扰 |
| **录制** | 手动开始/停止 | AI自动触发录制（信号出现时） | AI自动剪辑/标注录制内容 |
| **卫星跟踪** | 手动选择卫星 | AI自动计算过境并跟踪 | AI自动调度多卫星接收 |
| **天线控制** | 手动指向 | AI自动指向目标 | AI自动组成天线阵列 |
| **代码/算法** | 静态预定义 | AI推荐算法优化 | AI自动编写/优化/部署算法 |
| **人在回路** | 所有操作人确认 | 低风险自动，高风险人确认 | 核心修改人审核，其余自动 |

---

*本文档为 MBDSDR 项目的理论基础文档，将随项目进展持续更新。*
*v2.0 主要变更：修正"AI为定义主体"的激进表述为"人在回路、AI辅助"；拿掉"全平台"作为核心特征，降级为工程目标；自进化从必要条件降级为Roadmap方向；明确工具化双端定位（MCP + AI IDE用于coding）；增加能力演进三层框架；增加自然语言交互为核心特征。*

*v2.1 主要变更：特征四扩展为"工具化与可编程性"，新增源代码级自编程维度（运行时工具组合 + 源代码模块化可改可提交，自进化建立在此基础上）；新增1.1.1真实痛点分析（SDR已无处不在但软件仍停留在上一代，AIDR解决具体痛点，工具自明）；Level 3自进化安全约束新增"可审计审查流程"；新增7.8工程实现指引（MBDSDR项目的平台无关核心+即插即用抽象槽位+通用Agent接入引导）。*
