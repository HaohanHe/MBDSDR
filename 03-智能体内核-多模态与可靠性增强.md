# MBDSDR 智能体内核：通用 Agent 机制的 SDR 化落地

> 位置：`src/desktop/ai/`
> 定位：AI 定义无线电（AI-Defined Radio, AIDR）的"智能体操作系统层"。
> 方法：系统借鉴主流通用编码 Agent（Cline/Roo Code、Aider、Claude Code、
> ReAct/Reflexion 自修复范式）经过验证的机制，按无线电实时工况改造落地。

---

## 1. 机制对照表：从通用 Agent 到 SDR Agent

| 通用 Agent 机制 | 来源 | 通用场景作用 | MBDSDR 的 SDR 化落地 | 代码 |
|---|---|---|---|---|
| Context Engineering Harness + Focus Chain | Cline | 每轮精选指令/工具/文件/历史，只留相关 token | 能力地图按当前任务给 SDR 工具排序，当前频率/模式/设备永远锚定 | `CapabilityMap`、`ContextManager` |
| 影子 Git 检查点 | Cline / Roo Code | 每次工具改动后快照文件，任意时点回滚，不碰主仓库 | **运行时设备状态快照**：副作用动作（改频率/增益/模式/视图）前自动快照，连续失败自动回滚到"首次失败前的干净状态"，防 AI 幻觉把接收机调乱变砖 | `StateCheckpointManager` |
| Memory Bank（projectbrief/activeContext/progress） | Cline | 跨会话重建项目理解 | 频率库 / 设备档案 / 成功配方 / 干扰源档案，JSON 持久化，成功闭环自动沉淀"配方"复用 | `MemoryBank` |
| Repo Map（tree-sitter + PageRank + token 二分裁剪） | Aider | 把代码库符号压进预算，用户提到的标识符 10× 加权 | **能力地图**：工具按任务相关度加权排序、二分裁剪进 token 预算；设备能力+频谱态势作头部 | `CapabilityMap` |
| 弱模型会被复杂地图搞晕，故弱模型默认精简 | Aider FAQ | 弱模型误改 repo map 中的代码 | 模型分档 `ModelTier.WEAK/STRONG`：7B 只给工具名+一句、不给参数表；强模型给完整参数 | `CapabilityMap.build` |
| plan→act→observe→correct 四拍闭环 | ReAct / 通用 agent loop | 自主多步完成任务 | `AgentLoop`：LLM 决策动作→执行→观察→纠错，步数预算，逐步可追踪 | `AgentLoop` |
| 错误原样回灌、不隐藏 | Anthropic 工程实践 / Reflexion | 让模型看到原始错误自我修复 | 工具失败的原始 error 原样进下一步上下文；同工具连续失败 N 次注入"换方法"纠错提示并回滚 | `AgentLoop.run` |
| Sub-agent 独立上下文 | Claude Code | 派全新上下文子代理干重活，只回传结论，保护主上下文 | `SubAgent`：全频段扫描/逐段解调等大输出任务在子代理内完成，只回传压缩结论 | `SubAgent` |
| 长输出分页/滚动，保护上下文 | Warp / SWE-bench 方案 | 大文件只给窗口 | 频谱/IQ/截图走 `ExternalDataStore` 存引用，上下文只留摘要 | `ContextManager` |

---

## 2. 五大内核模块

### 2.1 AgentLoop —— 主控自修复闭环
- 四拍：`plan（LLM 出 thought+action）→ act（工具执行）→ observe（结构化结果）→ correct（失败回灌/换方法/回滚）`
- **错误分级处理**：
  - 单次失败：原始错误留在 observation，下一步模型自己看见并改（不打断）；
  - 同一工具连续失败达阈值（默认 2）：判定"钻牛角尖"，注入 Reflexion 纠错提示
    "不要重复同样调用，换工具/换思路"，并把设备状态回滚到该工具**首次失败前**的快照。
- **回滚锚点的关键细节**：必须锚定"连续失败序列第一次之前"的干净状态，而非最近一次
  （已被前一次失败污染的）快照——此缺陷在单测中被发现并修正。
- 步数预算（默认 12），耗尽返回 `budget_exhausted` 而非假装完成。

### 2.2 StateCheckpointManager —— 运行时状态检查点（防变砖）
- 与 Cline 影子 Git 的本质区别：Git 快照**文件**，这里快照**实时设备/解调状态**
  （频率、增益、模式、跨度、FFT、录制状态）。
- `ui_set_* / sdr_set_* / spectrum_set_* / start_recording` 等副作用工具执行前自动快照；
  只读工具不打快照。
- 支持 auto/manual/milestone 三种类型，任务开始打基线里程碑。

### 2.3 MemoryBank —— 跨会话记忆
四类持久记忆，落盘 `~/.mbdsdr/memory_bank.json`：
- `frequencies` 频率库：频率→台标/模式/备注/出现次数/最后见到；
- `devices` 设备档案：采样率/增益范围/已知坑；
- `recipes` 成功配方：自然语言目标→成功工具序列，**闭环成功后自动沉淀**，下次相似任务直接复用；
- `interference` 干扰源：频率→特征/方向/次数（服务于自动找干扰源）。
- 简单关键词相关检索（不引入向量依赖，离线/弱模型可用）。

### 2.4 CapabilityMap —— 能力地图（Aider Repo-Map 的 SDR 化）
- 相关性加权：任务词命中工具名 +10、命中描述 +3；当前状态相关类别上浮
  （正在录制则录制工具上浮、设备已连接则 sdr_ 工具上浮）。
- 二分搜索：在 token 预算内取"信息量最大且不超预算"的工具子集（目标贴预算上限）。
- 模型分档：弱模型极简（名+一句、限 14 个、不给参数表），强模型完整。

### 2.5 SubAgent —— 子代理上下文隔离
- 脚本式子代理执行确定性重活（如分段扫描全带宽），完整原始结果只留在子代理内，
  经 `summarize` 压缩后只把结论回传主代理，避免上千 bin 数据冲爆主上下文。

---

## 3. 为"免费 7B 小模型先调通"做的鲁棒性

策略：最弱的模型能用，强模型自然好用。`robust_parse_action` 容错处理小模型常见畸形输出：
1. ` ```json ` 代码围栏包裹；
2. JSON 前后带自然语言说明（抽取首个花括号配平对象）；
3. 单引号、中文引号 “”‘’；
4. 字段名混用：tool/action/function/name/调用工具、params/parameters/args/参数；
5. 纯文本最终答复（无工具意图）安全识别为 `done`；
6. 彻底无法解析时不崩溃，回灌一条"请只输出合法动作 JSON"的纠错，让模型下一轮自修。

业务级失败透传：工具处理器返回 `{"ok":false}` 时，`ToolCallManager` 不再误判为调用成功，
而是把失败透传给闭环触发纠错（此缺陷亦在测试中发现并修复）。

---

## 4. 模块与数据流

```
自然语言目标
   │
   ▼
AgentRuntime.run_goal
   ├── CapabilityMap.build(任务, 弱/强档) ── 工具相关度排序+token二分裁剪
   ├── MemoryBank.compact_text(任务)      ── 相关长期记忆注入
   ▼
AgentLoop（每一步）
   ├── 副作用前 StateCheckpoint 自动快照
   ├── LLM（鲁棒动作解析）
   ├── ToolCallManager（参数校验/钳制/重试/超时/模糊纠错）
   │        └── DesktopHttpInvoker → 127.0.0.1:7878 本地 API → 真实 SDR/频谱内核
   ├── 观察结果（错误原样回灌）
   └── 连续失败 → Reflexion 换方法 + 检查点回滚
   ▼
完成 → 成功工具序列沉淀为 MemoryBank.recipe
```

桌面端 GUI：`widgets/ai_panel.py` 的 `AgentWorker` 在后台 QThread 跑闭环，
每一步通过 Qt 信号回显（✓/✗、思考、工具、耗时），不阻塞界面。

---

## 5. 测试覆盖（全部离线可复现）

| 文件 | 自测内容 |
|---|---|
| `ai/agent_core.py` | 检查点快照/回滚锚点、记忆库持久化重载、能力地图排序/预算、闭环自修复+回滚、子代理隔离 |
| `ai/agent_runtime.py` | 5 种畸形动作解析、端到端多步闭环+配方沉淀、非法 JSON 自修 |
| `ai/http_tool_executor.py` | 13 个桌面工具注册、断连结构化错误不抛异常、参数钳制、模糊匹配 |
| 端到端集成 | 真实起 HTTP 服务+注入频谱帧+假 LLM，自主 3 步（总览→检测→maxhold 确认）闭环成功并真的从 HTTP 检出信号 |

运行：
```bash
cd src/desktop
python3 -m ai.agent_core
python3 -m ai.agent_runtime
python3 -m ai.http_tool_executor
```

---

## 6. 与论文创新点的关系

通用 agent 的四拍闭环、检查点、记忆、子代理本身不是创新；**创新在于把这套机制
锚定到无线电的物理实时约束上**：

1. **时间尺度失配分层**：LLM 秒级慢思考，而信号毫秒级瞬变——检查点保护的是
   物理设备状态，记忆库存的是物理频谱事实，子代理隔离的是高速数据流。
2. **主动感知驱动闭环**：`spectrum_suggest_next` 让闭环的"下一步观测"由信息增益
   决定，而不是人指定，形成"观测→判断→再观测"的物理实验闭环（贝叶斯最优实验设计）。
3. **不确定性自评估贯穿工具层**：每个检测结果带 confidence/persistence，闭环据此
   决定"信结论还是换 detector/换跨度再看一次"，这是通用 agent 没有的物理量纲。

闭环评测指标（配方复用率、平均闭环步数、自修复成功率、弱/强模型差距）由
`src/desktop/ai/agent_benchmark.py` 统一采集，可复现。

---

## 7. 真实免费小模型闭环实测（"先把弱模型调通"策略验证）

### 7.1 实验设置

- **被测模型**：`Qwen/Qwen2.5-7B-Instruct`（硅基流动，9B 以下永久免费档），
  刻意选用小模型——弱模型能跑通，强模型只会更好（Aider 经验：先服务最弱模型）。
- **方式**：本地起 HTTP 服务 + 注入已知频谱帧（真值已知），AgentRuntime 自主闭环，
  温度 0.1，模型不直接接触数值，只能通过工具拿数据。
- **评测脚本**：`python3 -m ai.agent_benchmark --mode real --rounds N`。
- **指标**：完成率、平均步数、解析失败次数、自纠错次数、频率命中率（结论频率 vs
  真值，容差 20~25kHz）、grounding 率（工具是否真在该频率观测到，区分
  "工具看到"与"模型答对"，防幻觉）。

### 7.2 实测中抓出并修复的三个真实问题

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 1 | 第二步起 API 400 | ReAct 历史用了 OpenAI 需 `tool_call_id` 严格配对的 `tool` 角色，Qwen 兼容模式拒绝 | 改为 assistant 动作 + user 观察回灌的纯文本 ReAct，不依赖 function-calling，兼容任意模型 |
| 2 | 小模型瞎编参数名（`start_frequency`） | 弱模型能力地图完全不给参数，模型只能猜 | 弱模型档给"合法参数名清单"但不给类型/范围细节，平衡"别被 schema 淹没"与"参数名正确" |
| 3 | **注入 1~2 个信号却报 5~10 个** | 单帧高斯噪声随机尖峰越过 8dB 门限（1024 点中总有连续 2 点越线） | find_signals 增加**两级确认**：强度余量 / 宽度 / 时间占空比三判据，瞬态噪声降为 candidate |
| 4 | **set_frequency 返回成功，检测却停在旧频段**（工具谎报成功，最危险） | `/api/set_frequency` 只改 `_status.frequency_hz`，未同步检测实际使用的 `_view_state.center_hz`，两份状态脱节 | set_frequency/set_samplerate 统一经 set_view_state 同步，单一事实源；跨频段测试固化防回归 |

> 问题 3 是可靠性层面的真实算法缺陷，不是演示问题。修复后单帧 20 个随机种子
> **零噪声误报**，多帧 max-hold 精确锁定 2 个真信号（duty=1.0）。
> 问题 4 是"工具成功 ≠ 物理生效"的典型，AI 若被谎报成功的工具误导会在错误频段
> 反复空转；修复后跨频段任务可自主切频并精确命中。

### 7.3 最终实测数据（Qwen2.5-7B，注入真值已知）

| 场景 | 完成率 | grounding 率 | 频率命中率 | 平均步数 | 解析失败 | 自纠错 |
|---|---|---|---|---|---|---|
| single（144.08MHz 单信号） | 100% | 100% | 100% | 1.0 | 0 | 0 |
| dual（433MHz 双信号） | 100% | 100% | 100% | 1.0 | 0 | 0 |
| cross_band（初始在空的144M，须自主切到433.05M） | 100% | 100% | 100% | 3.0 | 0 | 0 |

跨频段场景验证了副作用工具链：模型自主 `ui_set_frequency`（动作前自动打检查点）
→ 检测 → 精确报告，结论频率与真值误差仅个位数 Hz。

结论数值（如 144080207.2Hz / -43dBm）与注入真值一致，且都能在工具返回中溯源，
**证明结论来自工具而非模型幻觉**。免费 7B 在严格输出铁律 + 精简能力地图 +
两级确认检测的配合下，已能一步完成"自主检测→读真值→精确报告"闭环。

### 7.4 网络健壮性（跨环境可跑）

`APIClient` 实现代理自动选路与故障转移：环境变量代理 → 本地常见转发端口
(8118/7890/1087/6080) → 直连兜底，连接级错误自动换路、HTTP 业务错误不换。
保证用户家庭直连、公司代理、受限沙箱等不同网络下都能连通模型服务。

## 8. 检测模态自适应：数值为主、视觉为辅的双通道

纯数值检测（find_signals 返回结构化频率/功率）精确、可溯源、省 token，
但无法表达"信号长什么样"（调制形态、瀑布时间纹理、干扰外观）。为此引入
**对纯文本主模型透明的视觉通道**：

- `/api/screenshot` 默认把当前**真实频谱帧**渲染成 PNG 存盘
  (`~/.mbdsdr/screenshots/`)，只回路径与小元数据，避免 base64 冲爆 LLM 上下文；
  仅 `inline=1` 时内联图像。
- `APIClient.vision_analyze()` 调免费多模态模型 `Qwen/Qwen3-VL-8B-Instruct`
  （OpenAI image_url 协议，复用代理故障转移）。
- 工具 `vision_inspect_spectrum` 内部完成"inline 截图 → 解码 → VL 读图 → 文字"，
  主 LLM 即使是纯文本 7B，也只需调一个工具就能"看一眼频谱"。

实测（中心 145.8MHz / 跨度 480kHz，145.825MHz 注入一个峰）：VL 正确判断
"1 个峰、位于中心右侧、明显高出噪声"，方向与数量级正确；但其像素估读
（偏移约 +10kHz、高约 20dB）不如数值检测精确（真值 +25kHz、+37dB）。
这验证了架构取舍：**频率/功率等定量结论以数值工具为准，视觉只负责形态与纹理
等数值难以表达的维度**，二者由主模型自主编排融合。真实闭环中纯文本 7B 自主
按 find_signals → vision_inspect_spectrum 顺序调用并综合报告，双通道成立。

## 9. 真实闭环逼出的可靠性修复（离线测试发现不了）

### 9.1 工具失败 → 自主换路 / Reflexion

错误信息原样回灌（而非笼统报"失败"）后，真实 7B 倾向于**第一次失败就自主换
另一条工具路径**，比"连续 2 次失败才强制 Reflexion"更高效。实测一条完整链：
`spectrum_set_view` 失败 → `analyze_range` 失败 → 换 `get_view` 成功 → 正确报告
中心信号。连续同一工具失败 2 次的系统纠错 + 检查点回滚作为兜底保留（离线已验证）。

### 9.2 中心频率信号被 DC 抑制误杀（致命边界）

零中频接收机的 DC spike 抑制，原逻辑"中心 bin 比邻域高 >6dB 就整段插值删除"，
会把**恰好位于中心频率的真实信号**一并删掉——而 HAM 最常把目标信号调到正中。
重写为按中心凸起宽度分界：凸起半宽 ≤1 bin（整峰 ≤3 bin，符合 DC 经窗函数的
能量集中范围）判为 DC，精确删除匹配宽度；凸起更宽判为中心真信号，只把最中心
1 bin 用两翼插值、保留信号主体。验证：纯 DC 单点/3-bin 正确删除，6-bin 与 15-bin
中心信号正确保留，8 工况全过。诚实边界：≤3-bin 的正中 CW 与 DC 在单帧频谱上
形态不可区分，彻底根治需时域 IQ 去均值（已列 Roadmap），频谱层采取"不误杀正中
目标"的保守策略。
