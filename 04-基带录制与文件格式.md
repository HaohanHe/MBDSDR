# MBDSDR 基带录制与文件格式（IO 子系统）

> 模块：`src/desktop/dsp/baseband_recorder.py`（独立于 GUI，headless / AI / 设备线程均可写盘）
> 接入：`http_server.py` 的 `/api/start_recording`、`/api/stop_recording`；AI 工具 `ui_start_recording` / `ui_stop_recording`
> License: GPL-3.0-or-later

## 1. 设计目标

SDR 软件的基本能力之一是把原始基带（IQ）可靠落盘，供事后解调、回放、数据集构建与论文复现。
MBDSDR 的录制器对齐 GNU Radio / SDR++ / SDR# / SatDump 生态的主流交换格式，并满足：

1. **多格式**：cf32 / cs16 / wav / csv 一次实现，用户与 AI 按需选择；
2. **颗粒度可控**：采样率、抽取因子（decimation）、定点增益均可指定；
3. **可回放**：每个录制文件配套同名 `.json` sidecar，记录中心频率/采样率/格式/样本数/时间，
   文件离开本机仍能被正确解析；
4. **AI 可直接操作**：纯文本一句话即可启动定时录制，到点服务端自动停，不依赖模型自己 sleep；
5. **安全稳健**：线程安全、软上限防爆盘、相对路径自动归位、扩展名自动补齐。

## 2. 四种格式

| 格式 | 内存布局 | 每样本字节 | 适用场景 |
|---|---|---|---|
| `cf32` | 交错 float32 小端 `[I0,Q0,I1,Q1,...]` | 8 | 最通用的复数浮点原始流，GNU Radio/SDR++ 默认，无损、处理方便（**默认**） |
| `cs16` | 交错 int16 小端 | 4 | 省一半空间的 16bit 定点原始流，长时采集/嵌入式 |
| `wav` | 双声道 16bit PCM，I=左、Q=右，采样率写入 WAV 头 | 4(+44B 头) | SDR# 风格，任意音频/审计工具可直接打开 |
| `csv` | 文本行 `I,Q`（首行表头） | 文本 | 教学、表格审计、小规模数据；建议配合大抽取 |

浮点输入统一归一化到 `[-1,1]`，写 cs16/wav 时按 `clip(x*gain,-1,1)*32767` 量化。

## 3. 颗粒度参数

- `sample_rate_hz`：标称采样率，写入 WAV 头与 sidecar（回放必需）。默认取当前接收机采样率。
- `decimation`：录制抽取因子，每 N 个样本取 1 个，`effective_rate = sample_rate/decimation`，
  等价于在录制侧做降速，直接决定文件体积与有效带宽。
- `gain`：浮点转 int16 前的归一化增益（仅 cs16/wav 有意义）。
- `duration_s`：定时录制秒数；>0 时由服务端 `threading.Timer` 到点自动停止并落盘，
  AI 无需也不应自己空等。

## 4. sidecar 元数据（可回放的关键）

录制停止时写 `<文件>.json`：

```json
{
  "path": ".../mbdsdr_145800kHz_20260913-203000.cf32",
  "format": "cf32",
  "center_hz": 145800000,
  "sample_rate_hz": 480000,
  "decimation": 2,
  "effective_rate_hz": 240000,
  "samples": 50000,
  "bytes": 400000,
  "duration_s": 0.2083,
  "start_unix": 1789000000.0,
  "end_unix": 1789000000.208,
  "note": "wav: I=ch1,Q=ch2; cf32/cs16: interleaved little-endian"
}
```

## 5. HTTP / AI 接口

- `POST /api/start_recording`
  body: `{format, path?, sample_rate_hz?, decimation?, gain?, duration_s?}`
  返回：`{ok, path, format, sample_rate_hz, decimation, effective_rate_hz, hint?}`。
  定时录制时 `hint` 明确告知"到点自动停，无需紧接着 stop"，避免弱模型立刻停止空录制。
- `POST /api/stop_recording` → `{ok, stats}`，stats 即 sidecar 内容。
- 设备/HAL/模拟器通过 `bridge.push_iq_samples(iq)` 喂入复数 IQ（或 `(I,Q)` 两序列、交错数组），
  录制中自动写盘；未录制时为空操作。接受复数 ndarray、`(I,Q)` 元组、交错实数组三种形态。
- AI 工具 `ui_start_recording / ui_stop_recording`（category=`record`），
  能力地图在"录制中"自动上浮录制类工具。

## 6. 安全与健壮性

- **路径规整**：裸文件名/相对路径自动落到 `~/.mbdsdr/recordings/`，不污染工程目录；
  扩展名缺失或与格式不符（如给 cs16 却写 .wav）自动纠正。
- **防爆盘**：`max_bytes`（默认 2GB）软上限，到达后按样本帧对齐截断并自动收尾，不会写半个样本。
- **线程安全**：设备线程写、控制线程停，用锁保护；重复 stop 幂等。
- **单录约束**：启动新录制前自动收尾旧录制。

## 7. 验证（离线可复现）

`python3 dsp/baseband_recorder.py` 自测覆盖：

- 四格式分两批增量写、抽取后样本数正确（10000 抽 2 → 5000）；
- cf32 回读：交错 float32、I 通道均值误差 <1e-4；
- wav 回读：双声道、16bit、采样率=有效速率、帧数正确（RIFF 头由 `wave.writeframes` 正确生成）；
- 1KB 软上限截断、sidecar JSON 字段完整。

HTTP 端到端实测：四格式、抽取、sidecar 全通过；定时录制 0.5s 实测 0.52s 自动停、
样本数 = 速率×时长（100k×0.5=50000）、统计时长 0.500s。真实 Qwen2.5-7B 闭环：
模型自主选择 cf32、设置抽取、启停并报告路径/样本数/时长。

## 8. 回放读取与离线文件分析（录制的逆过程）

模块 `dsp/baseband_reader.py`，与录制器对称，形成"录制→落盘→回放/分析"闭环：

- `read_baseband(path)`：四格式读回复数 IQ，自动从 sidecar/扩展名推断格式，
  支持 `offset/max_samples` 按样本截取；cf32 近无损（最大误差 ~3e-8），
  cs16/wav 量化误差 ~6e-5，csv ~7e-7（自测实测）。
- `iter_baseband(path, chunk)`：流式分块读取，cf32/cs16 按字节偏移不全量载入，
  可把文件模拟成"虚拟 SDR 源"实时回放，喂给解调链。
- `analyze_baseband_file(path, nfft)`：**AI 拿到任意基带文件即可读懂内容**——
  多帧平均周期图（Welch，Hanning 窗，最多 64 帧）估计功率谱、峰值归零相对化，
  复用 `find_signals` 两级确认检测，返回中心/采样率/时长/噪声底/占用率/信号列表。
  自测：500k 采样率下 +20kHz 音调精确检出（+20.0kHz），-30kHz 实测 -29.8kHz。
- HTTP：`POST /api/analyze_baseband {path, format?, nfft?}`；
  AI 工具 `baseband_analyze_file`（category=record）。

这一闭环对论文复现至关重要：任何实验都能录成基带文件，离线逐帧重放、重复分析，
结果可复现；也让纯文本 AI 能对一段历史采集做事后排查（"回旋镖"原则：工具产出的
数据，模型自己也能再读回来分析）。

## 9. 与后续工作的衔接

- 当前 IQ 由设备流/模拟器 `push_iq_samples` 驱动；接入真实 HAL 后，设备回调直接喂入即可，录制路径不变。
- 时域 IQ 直流去除/正交不平衡校正应在写入前的 IQ 链完成（与频谱层 DC 抑制互补，见智能体内核文档 9.2）。
- 回放器已可流式产出 IQ，下一步把 `iter_baseband` 封装为虚拟 SDR Source（与真实设备同一 HAL 接口），实现"文件即设备"的离线解调。
