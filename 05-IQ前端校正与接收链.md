# MBDSDR IQ 前端校正与接收链（时域损伤处理）

> Python 模块：`src/desktop/dsp/iq_frontend.py`（AI 工具链自包含、离线可跑）
> C++ 对应：`src/dsp` 的 `dc_blocker / iq_calibrator / decimator / fir_filter / multi_ddc / agc*`（实时高性能）
> License: GPL-3.0-or-later

## 1. 为什么要在时域做，而不是频谱上删 bin

零中频（ZIF）架构的 SDR 前端有三类固有损伤。早期版本只在功率谱上"删除中心几个 bin"
来掩盖直流尖峰，这会误杀恰好位于中心的真实信号（HAM 最常把目标调至正中），且无法处理
I/Q 镜像。频谱层删 bin 只是显示兜底，**根治必须在 IQ 时域**：

| 损伤 | 成因 | 时域处理 | 频谱删 bin 为何不够 |
|---|---|---|---|
| 直流偏置 DC offset | 本振自混频泄漏 | DC blocker 跟踪并减去均值 | 正中真信号与 DC 单帧不可区分，只能猜 |
| I/Q 不平衡 | 两路增益/相位不一致 | 协方差白化去相关 | 产生关于中心对称的镜像，删中心无效 |
| 过采样冗余 | 采样率高于所需带宽 | 抗混叠 FIR + 整数抽取 | 频谱只是显示缩放，数据率没降 |

## 2. DC 阻断（DCBlocker）

一阶 IIR 标准结构：`y[n] = x[n] - x[n-1] + R·y[n-1]`，R∈[0,1)，典型 0.995~0.999。
相比"减整段均值"，它能跟踪随温度/时间缓慢漂移的直流，且流式、O(1) 状态、无需整段已知。
实测：注入均值幅度 1.0 的直流，阻断后残留 2.1e-3（跳过初始暂态），中心不再有固定尖峰，
也就不再需要"猜中心是 DC 还是信号"。

## 3. I/Q 平衡：协方差白化（关键设计选择）

常见做法是分别反解增益差 g 与相位误差 φ 再补偿，但 g 与 φ 在二阶矩里相互耦合，
逐参数反解不彻底（实测仅改善约 10dB）。本实现改用**协方差白化**：

1. 去均值后估计 2×2 协方差 `C = [[varI, covIQ],[covIQ, varQ]]`；
2. 特征分解 `C = VΛVᵀ`，构造白化矩阵 `W = V·diag(1/√Λ)·Vᵀ`；
3. 对 `[I,Q]ᵀ` 左乘 W，使两路零均值、等功率、互不相关（`W C Wᵀ = I`）。

白化对任意"增益差 + 正交相位误差"组合都成立，不依赖参数化假设。
实测：注入 Q 增益 -20%、相位误差 12°，镜像抑制比 **IRR 16.7dB → 102.3dB**
（数值精度极限，镜像基本完全消除）；诊断量估计相位 12.1°、增益 0.098，与注入一致。
fit 一次得到 W 后可对后续连续流 apply，流式友好。

## 4. 整数抽取（decimate）

先 Hanning 窗 sinc 低通（截止 0.5/factor 抗混叠）再每 factor 取 1，输出速率=输入/factor。
这是 DDC（数字下变频）的末级；频率搬移（混频到基带）由 NCO/rotator 负责，C++ 侧
`multi_ddc` 已实现多通道完整版本，Python 侧提供离线分析所需的抽取。实测 x4 抽取后
主峰频率精确保留（100.0kHz）。

## 5. 一站式 front_end 与接入点

`front_end(x, dc_r, correct_iq, decimation)` 顺序执行 DC→IQ 平衡→抽取，返回
(校正 IQ, 诊断)。已接入基带文件分析 `analyze_baseband_file(..., correct_iq=True)`：
AI 用 `baseband_analyze_file` 工具分析任意录制文件时，默认先做时域校正再 Welch 周期图检测，
返回中带 `iq_correction: {iq_gain_err, iq_phase_err_deg}` 诊断，因此：

- 频谱中心不再有 DC 残留，正中信号不会被误删；
- I/Q 镜像被白化消除，不会把镜像当成第二个真实信号；
- AI 能直接看到"这段基带的前端损伤有多大"，作为数据质量判断依据。

## 6. 与 C++ 实时链的分工

- **实时路径**（设备→解调→扬声器/解码）：走 C++ `src/dsp`，SIMD/零拷贝、确定性时延；
- **离线/AI 路径**（录制文件分析、数据集清洗、论文复现）：走本 Python 模块，无需编译、
  与 NumPy 生态和智能体工具链无缝衔接。
- 两侧算法等价、互为验证；Python 侧用于快速实验与论文原型，成熟后可平移到 C++。

## 7. 验证（离线可复现）

`python3 dsp/iq_frontend.py`：DC 残留、IRR 改善（16.7→102.3dB）、抽取速率与主峰、
一站式诊断四项断言。`baseband_reader.py` 自测覆盖"含 DC+I/Q 损伤文件→校正→检测"链路。

## 8. 后续

- AGC（自动增益）在 C++ 已有 `agc/agc_pro`，Python 离线链可补对应归一化用于电平标准化；
- 频偏估计与校正（Costas/旋转器）接入后，可在前端把信号自动搬到零频，服务"AI 自动核对中心频点"；
- 真实设备 IQ 接入后，前端校正同时作用于"实时频谱显示"与"录制落盘前"，保证显示与录制同源同质量。
